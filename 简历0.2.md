# 万守洋

**求职意向：Java 后端开发实习生** ｜ 手机：17839330662 ｜ 2027 届本科 ｜ 可实习 3个月以上

## 教育背景

**河南农业大学** ｜ 软件工程 · 本科 ｜ 2023.09 – 2027.06(2027 届)

主修课程：数据结构、计算机网络、操作系统、数据库原理、Java 程序设计、算法设计与分析、软件工程

## 专业技能

- **Java 基础**：掌握集合框架、IO、反射与常用设计模式(策略、模板方法等在项目中有实际落地)；了解 JVM 内存结构、类加载机制与垃圾回收。
- **并发编程**：熟悉线程池、ThreadLocal、CAS、AQS 等机制，实践过 ThreadLocal 用户上下文传递、数据库乐观锁与 Redisson 分布式锁。
- **后端框架**：熟练使用 Spring Boot 3、Spring MVC、MyBatis、MyBatis-Plus，熟悉 RESTful API 设计、参数校验、全局异常处理与统一响应封装。
- **微服务**：熟悉 Spring Cloud 体系(Nacos、Gateway、OpenFeign、Sentinel、Seata)，理解服务拆分、网关鉴权与分布式事务的落地方式。
- **数据库与缓存**：熟练使用 MySQL，了解索引、事务、锁与 SQL 优化；熟悉 Redis 常用数据结构，掌握缓存穿透/击穿/雪崩治理与 Caffeine + Redis 多级缓存实践。
- **中间件**：熟悉 RabbitMQ(异步解耦、事务后发消息)，使用过 Elasticsearch、XXL-JOB、ShardingSphere-JDBC；了解 Docker、Nginx 与 Linux 常用命令。
- **开发工具**：熟练使用 Git、Maven、IDEA、Apifox，能够使用 Swagger(springdoc)/Knife4j 维护接口文档。

## 项目经历

### novel 在线小说平台(单体架构)

**2026.05 – 至今** ｜ Spring Boot 3 · JDK 21 · MyBatis-Plus · MySQL 8 · Redis · Caffeine · Redisson · JWT · Elasticsearch · RabbitMQ · XXL-JOB · Sentinel · Spring AI

前后端分离的小说业务系统，含小说门户、作家后台、平台管理后台三个业务端，覆盖推荐、检索、阅读、评论、书架、充值订阅等场景。本人在开源学习型项目基础上二次开发，独立完成平台管理后台的后端实现，并深入实践缓存、鉴权、搜索、消息等工程能力。

- **多端认证鉴权**：基于 JWT + 拦截器 + 策略模式实现门户/作家/管理三端权限隔离，扩展管理端认证策略(账号状态校验、按系统标识区分令牌)；ThreadLocal 保存用户上下文并在请求结束后清理，避免线程复用导致数据串用。
- **多级缓存**：基于 Caffeine + Redis 双 CacheManager 按缓存域划分本地/分布式缓存并配置差异化 TTL，通过 Spring Cache 注解覆盖首页推荐、小说详情、章节内容等高频读场景，降低数据库压力；写操作后主动失效缓存保证一致性。
- **接口限流与分布式锁**：利用 Sentinel 配置全局 QPS 匀速排队与单 IP 秒级/分钟级参数限流实现接口防刷；基于 Redisson + 自定义 @Lock 注解 + AOP + SpEL 实现声明式分布式锁，防止关键业务并发重复执行。
- **搜索与数据同步链路**：使用 Elasticsearch 实现全文检索(bool 组合查询、高亮、分页排序)；小说变更后经 RabbitMQ 异步增量同步至 ES，借助 Spring 事务同步器保证"事务提交后再发消息"；XXL-JOB 定时任务做游标分批 + bulk 批量写入的全量同步兜底。
- **安全与工程化**：XSS 过滤器 + 请求包装器净化入参，AOP 校验排序字段白名单防止 SQL 注入；集成 Spring AI 实现作家后台 AI 扩写/缩写/续写/润色；springdoc 维护接口文档，配置全局异常处理与日志采集。

### 万佳商城微服务系统

**2026.03 – 2026.05** ｜ Spring Boot · Spring Cloud Alibaba · Nacos · Gateway · OpenFeign · Sentinel · Seata · MyBatis-Plus · MySQL · Redis · RabbitMQ · Knife4j

B2C 电商系统，从单体重构为微服务架构，包含商品、购物车、用户、交易、支付五个业务服务与统一网关，实现服务注册发现、网关鉴权、服务间通信、分布式事务与异步消息通知。

- **服务拆分与基础设施**：参与单体到微服务的拆分，梳理商品/购物车/用户/交易/支付的模块边界；基于 Nacos 实现服务注册发现与多环境共享配置。
- **统一网关鉴权与动态路由**：编写 Gateway 全局过滤器实现 JWT 登录校验、白名单放行与用户身份透传；基于 Nacos 配置监听器实现网关动态路由加载，路由规则变更无需重启即可生效。
- **公共模块沉淀**：抽取 hm-common 组件，通过 spring.factories 自动装配 MVC/MyBatis/MQ/JSON 配置；拦截器 + ThreadLocal 还原网关透传的用户上下文；统一全局异常处理与分页响应，消除多服务重复代码。
- **服务间调用与容错**：基于 OpenFeign 实现商品查询、库存扣减、购物车清理、余额扣减等远程调用；Feign 请求拦截器透传用户信息，解决跨服务上下文丢失；FallbackFactory 实现降级逻辑，结合 Sentinel 提升容错能力。
- **下单主链路与分布式事务**：实现订单创建流程(商品校验、金额计算、订单与明细落库、清理购物车、扣减库存)，使用 Seata @GlobalTransactional 保证跨服务数据一致性，库存扣减采用数据库原子更新语句。
- **支付与异步解耦**：实现支付单幂等校验、余额扣减与基于状态条件更新的乐观锁标记支付成功；支付成功后通过 RabbitMQ 通知交易服务更新订单状态，实现服务解耦，消息发送失败记录日志降级，不阻塞支付主流程。

## 自我评价

- 具备扎实的 Java 后端基础，能独立完成模块从接口设计、编码到联调的完整闭环；做功能前习惯先对齐现有项目的规范与分层。
- 对底层原理有钻研意愿，缓存、消息队列、分布式锁等组件会主动读文档与源码理解机制；能快速上手已有代码库做增量开发，乐于接受评审意见并快速迭代。
