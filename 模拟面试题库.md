# 模拟面试题库(基于简历逐条出题,含参考答案)

> 使用说明:本题库完全按照你简历的写法出题——面试官就是这么干的,简历上每一句话都是考点。
> 每题包含【面试官提问】和【参考答案】,部分题带【追问】,追问链是大厂面试的常态。
> 标注 ⚠️ 的题,是你项目代码里真实存在的薄弱点,面试官如果看过类似项目一定会挖,答案里给了"诚实 + 展示思考"的应对话术。
> 答案用第一人称写,方便直接内化成自己的话。**切忌背诵腔,理解后用自己的话说。**

---

## 一、Java 基础与 JVM

### Q1:HashMap 的底层结构?为什么线程不安全?

**参考答案:**
JDK 8 以后是数组 + 链表 + 红黑树。put 时对 key 的 hashCode 做扰动运算后与 (length-1) 按位与定位桶;链表长度超过 8 且数组长度达到 64 时链表转红黑树,避免哈希冲突严重时查询退化成 O(n)。扩容时容量翻倍,元素按"原位置"或"原位置+旧容量"迁移。

线程不安全的表现:多线程同时 put 可能丢失数据(两个线程同时写同一个桶,后写覆盖先写);JDK 7 头插法在并发扩容时还会造成链表成环导致死循环。并发场景用 ConcurrentHashMap,JDK 8 里它用 CAS + synchronized 锁单个桶头节点,锁粒度小,并发度高。

**追问:为什么负载因子是 0.75?**
是空间和时间的折中:太大冲突概率高,太小浪费空间、频繁扩容。

### Q2:简历里写"策略、模板方法等设计模式在项目中有实际落地",具体讲讲?

**参考答案:**(这题必考,因为写在简历第一条,答案直接对应你的代码)
策略模式:novel 的多端鉴权。定义了 `AuthStrategy` 接口,门户端、作家端、管理端各自实现(`FrontAuthStrategy`、`AuthorAuthStrategy`、`AdminAuthStrategy`)。拦截器里注入的是 `Map<String, AuthStrategy>`——Spring 会把所有实现类按 bean 名收进这个 Map,拦截器从请求 URI 中解析出系统前缀(如 admin),拼出 bean 名 `adminAuthStrategy` 直接取对应策略执行。好处是新增一个业务端只需要加一个策略类,不用改拦截器,符合开闭原则。

模板方法:novel 的消息发送模块,`AbstractMessageSender` 抽象类定义了发送流程骨架,邮件、系统通知等子类只覆写各自的发送细节。

**追问:策略模式和一堆 if-else 相比,代价是什么?**
类数量变多、间接性增强,简单场景反而过度设计;适合"分支会持续增加、每个分支逻辑独立"的场景,鉴权按端划分正是这种。

### Q3:反射了解吗?你项目里哪里用到了?

**参考答案:**
反射是运行期获取类结构、调用方法/构造器的能力。我在分布式锁切面 `LockAspect` 里用到:通过 `MethodSignature` 拿到目标 `Method`,再读取方法上的 `@Lock` 注解和参数上的 `@Key` 注解,结合 SpEL 解析出锁的 key。另外 Spring 的依赖注入、MyBatis 把结果集映射成实体,底层都是反射。缺点是绕过编译期检查、有性能开销,框架里一般会做缓存。

### Q4:JVM 内存区域划分?哪些区域会 OOM?

**参考答案:**
线程私有:虚拟机栈(方法调用栈帧,深递归会 StackOverflowError)、本地方法栈、程序计数器。线程共享:堆(对象实例,最常见 OOM:`Java heap space`)、方法区/元空间(类元数据,JDK 8 后在本地内存,类加载过多会 `Metaspace` OOM)。此外堆外的直接内存(NIO)也可能 OOM。除程序计数器外都可能内存溢出。

### Q5:类加载过程和双亲委派?为什么要双亲委派?

**参考答案:**
加载 → 验证 → 准备 → 解析 → 初始化。双亲委派:类加载器收到请求先委托父加载器,父加载不了才自己加载,链路是 App → Ext(JDK9 后 Platform)→ Bootstrap。目的一是避免类被重复加载,二是安全——防止用户自定义 `java.lang.String` 冒充核心类。打破的例子:JDBC 的 SPI(启动类加载器需要调用应用类加载器加载的驱动,用线程上下文类加载器)、Tomcat 的 Web 应用隔离。

### Q6:垃圾回收:怎么判断对象可以回收?G1 了解吗?

**参考答案:**
可达性分析:从 GC Roots(栈上引用、静态变量、常量、JNI 引用等)出发,不可达的对象可回收;不用引用计数是因为解决不了循环引用。

分代:新生代(Eden + 两个 Survivor,Minor GC,复制算法)、老年代(Major/Full GC,标记-整理)。G1 是 JDK 9 后的默认收集器,把堆划分成等大的 Region,可以预测停顿时间(-XX:MaxGCPauseMillis),回收时优先收集垃圾占比高的 Region(Garbage First 得名于此)。我的 novel 项目用 JDK 21,默认就是 G1。

**追问:JDK 21 你们开了虚拟线程,说说?**(你 application.yml 里 `spring.threads.virtual.enabled: true`,这是真实配置,大概率被问)
虚拟线程是 JDK 21 转正的轻量级线程,由 JVM 调度、挂在少量平台线程(载体线程)上跑,阻塞时会自动让出载体线程,所以适合 IO 密集型场景。Spring Boot 开启后,Tomcat 处理请求不再用固定 200 的平台线程池,而是每个请求一个虚拟线程,高并发下阻塞等待(查库、调外部接口)不再占满线程池。注意点:不适合 CPU 密集任务;`synchronized` 块里阻塞会"钉住"(pinning)载体线程(JDK 21 的限制),所以更推荐用 ReentrantLock;ThreadLocal 依然可用但海量虚拟线程时要注意内存。

---

## 二、并发编程

### Q7:ThreadLocal 原理?你项目里怎么用的?为什么一定要清理?

**参考答案:**(两个项目都用了,必考)
每个 Thread 内部有一张 ThreadLocalMap,key 是 ThreadLocal 对象(弱引用),value 是存的值。set/get 都是操作当前线程自己的 map,所以天然线程隔离。

项目里两处:novel 的 `UserHolder`、hmall 的 `UserContext`,都是鉴权通过后把 userId 存进 ThreadLocal,后续 Service 层随取随用,不用层层传参。

必须清理的原因:Tomcat 用线程池,线程会被复用。请求 A 结束不清理,线程被请求 B 复用时会读到 A 的 userId,造成串号——这是数据安全事故。另外 key 是弱引用会被 GC 回收,但 value 还被线程强引用着,长期不清理会内存泄漏。所以我在拦截器的 `afterCompletion` 里统一 remove,这个时机保证无论业务是否抛异常都会执行。

**追问:为什么放 afterCompletion 而不是 postHandle?**
postHandle 在异常时不会执行,afterCompletion 无论如何都会回调,清理必须放在这里。

### Q8:线程池的核心参数和执行流程?怎么设定线程数?

**参考答案:**
七个参数:核心线程数、最大线程数、空闲存活时间、时间单位、工作队列、线程工厂、拒绝策略。流程:任务来了先用核心线程 → 核心满了进队列 → 队列满了开非核心线程 → 到达最大线程数后触发拒绝策略(AbortPolicy 抛异常 / CallerRunsPolicy 调用者自己跑 / Discard 系列丢弃)。

线程数经验值:CPU 密集型 ≈ 核数+1;IO 密集型 ≈ 核数 × (1 + 等待时间/计算时间),再结合压测调整。阿里规约不建议用 Executors 快捷方法,因为 newFixedThreadPool 的无界队列和 newCachedThreadPool 的无限线程都可能 OOM,应手动 new ThreadPoolExecutor。

### Q9:乐观锁和悲观锁?你项目里分别在哪用了?

**参考答案:**
悲观锁假设冲突一定发生,先加锁再操作,如 synchronized、数据库 `select for update`;乐观锁假设冲突少,提交时校验,失败重试或放弃,如 CAS、数据库带条件的 update。

项目实践:hmall 支付单标记成功用的是乐观锁——`UPDATE pay_order SET status = 3 WHERE id = ? AND status IN (0, 1)`,把状态判断放进 WHERE 条件,返回影响行数为 0 说明已被并发修改过,直接判定失败,防止重复标记;novel 里防止关键业务并发重复执行用的是 Redisson 分布式锁,属于悲观思路。

### Q10:CAS 和 AQS 简单说说?

**参考答案:**
CAS:比较并交换,CPU 原子指令,拿期望值和内存值比较,相等才更新,失败自旋重试;问题有 ABA(AtomicStampedReference 加版本号解决)、长时间自旋浪费 CPU、只能保证单个变量原子性。

AQS:AbstractQueuedSynchronizer,JUC 锁的基础框架。核心是一个 volatile int state + 一个 CLH 变体的 FIFO 等待队列:抢锁就是 CAS 改 state,抢不到就包装成节点入队挂起,释放时唤醒后继节点。ReentrantLock、CountDownLatch、Semaphore 都基于它,区别只在对 state 的语义定义(重入次数/计数/许可数)。

---

## 三、MySQL

### Q11:为什么用 B+ 树做索引,而不是 B 树或哈希?

**参考答案:**
B+ 树非叶子节点只存键不存数据,单页(16KB)能放更多键,树更矮,磁盘 IO 更少;数据全在叶子层且叶子间有双向链表,范围查询和排序只需顺着链表扫。B 树数据分散在所有节点,范围查询要中序回溯;哈希只支持等值,不支持范围和排序。

**追问:聚簇索引和二级索引?什么是回表?**
InnoDB 主键索引的叶子存整行数据(聚簇);二级索引叶子只存主键值,查到主键后还要回主键索引再查一次整行,这叫回表。用覆盖索引(查询列都在索引里)可以避免回表——这也是为什么不建议 `select *`。

### Q12:你项目里的一条 SQL 变慢了,怎么优化?

**参考答案:**
先用 `EXPLAIN` 看执行计划,关注 type(至少 range,出现 ALL 全表扫描要警惕)、key(实际用的索引)、rows(预估扫描行数)、Extra(Using filesort / Using temporary 说明排序或临时表没走索引)。常见手段:给 WHERE / ORDER BY 列建合适的联合索引并满足最左前缀;避免在索引列上做函数运算或隐式类型转换(如字符串列传数字)导致索引失效;只查需要的列争取覆盖索引;大分页改游标方式。

结合项目:novel 里查小说首章是 `WHERE book_id = ? ORDER BY chapter_num LIMIT 1`,给 `(book_id, chapter_num)` 建联合索引,查询和排序都能命中,这类"等值 + 排序"场景联合索引顺序很关键。

### Q13:深分页为什么慢?你项目里怎么处理的?

**参考答案:**(项目里有现成实践,一定要主动说)
`LIMIT 1000000, 30` 要扫描并丢弃前一百万行,offset 越大越慢。我在 novel 的 ES 全量同步任务里用的是游标法:记录上一批最大 id,每次 `WHERE id > #{maxId} ORDER BY id LIMIT 30`,利用主键索引直接定位起点,每批成本恒定,百万级数据同步也不会越跑越慢。对外的分页接口如果允许,也可以改成"基于上一页最后一条记录的游标分页",不允许就限制最大页码。

### Q14:事务隔离级别?MySQL 默认哪个,怎么实现的?

**参考答案:**
读未提交(脏读)、读已提交(不可重复读)、可重复读(幻读)、串行化。MySQL 默认可重复读(RR)。实现靠 MVCC + 锁:每行有隐藏的事务 id 和回滚指针,undo log 构成版本链,事务根据 ReadView 判断哪个版本可见——RC 每次查询生成新 ReadView,RR 只在第一次查询生成,所以能重复读。RR 下的幻读由间隙锁(Next-Key Lock)在当前读时阻止。

---

## 四、Redis 与缓存(结合 novel 多级缓存,重点)

### Q15:为什么要 Caffeine + Redis 两级缓存?只用 Redis 不行吗?

**参考答案:**
两级的分工不同。Caffeine 是进程内缓存,读的是本机内存,纳秒级、零网络开销,但容量受堆限制、多实例间不共享;Redis 是集中式缓存,毫秒级,多实例共享、容量大。novel 里我按"缓存域"划分:小说分类这种几乎不变的、首页推荐这种全站同一份且读极高频的放 Caffeine;和用户相关的、需要多实例一致的放 Redis。实现上是配置了两个 CacheManager(Caffeine 设为 @Primary),每个缓存域在枚举里声明自己是本地还是远程、maxSize 和 TTL 是多少,业务代码只用 `@Cacheable` 注解并指定 cacheManager,不感知细节。

**追问 ⚠️:本地缓存在多实例部署时数据不一致怎么办?**
(诚实版)目前项目是单机部署所以没暴露这个问题,但方案我考虑过:一是给本地缓存设短 TTL,容忍秒级不一致;二是数据变更时通过 MQ 广播失效消息,各实例监听后清掉自己的本地缓存——novel 里已有小说变更消息(用于 ES 同步),再加一个本地缓存失效的消费者就能实现。

### Q16:缓存穿透、击穿、雪崩,你项目里分别怎么应对?

**参考答案:**
穿透:查不存在的数据,缓存永远不命中,每次都打到 DB。方案:缓存空值(设短 TTL)、布隆过滤器拦截非法 id、入参校验(id 格式/范围)。
击穿:某个热点 key 过期瞬间,大量请求同时打到 DB。方案:互斥锁重建(只放一个请求去查库,其他等待)、热点数据逻辑过期(不设 TTL,值里带过期时间,发现过期异步重建)。
雪崩:大量 key 同时过期或 Redis 宕机。方案:TTL 加随机值打散、热点数据多级缓存兜底、Redis 高可用(主从+哨兵/集群)、限流降级保护 DB。

⚠️ 结合项目要诚实:novel 配置里 `disableCachingNullValues`,即不缓存空值,严格说存在穿透风险;我的补救思路是对"按 id 查详情"这类接口加入参合法性校验,并且可以对确认不存在的 id 缓存一个短 TTL 的空对象标记(需要允许缓存 null 或用特殊哨兵值)。主动指出自己项目的缺陷并给出方案,是加分项。

### Q17:缓存和数据库的一致性怎么保证?你项目里怎么做的?

**参考答案:**
通用结论:先更新数据库,再删除缓存(Cache Aside),配合 TTL 兜底。不用"先删缓存再更新库"(并发下旧值会被回填),也不建议"更新缓存"(写写并发会乱序,且缓存的是加工后的对象,更新成本高)。极端场景(删缓存失败)用消息队列重试删除,或订阅 binlog(Canal)异步删。

项目里:小说信息更新后调用 `@CacheEvict` 标注的方法主动失效缓存,下次读时回源重建;同时每个缓存域都有 TTL,即使漏删也能最终一致。

**追问:为什么"先更新库再删缓存"也不是绝对安全?**
读请求在缓存 miss 后查了旧值、还没写回缓存时,写请求完成了更新和删除,然后读请求把旧值写回——理论上有这个窗口,但要求"读比写慢",概率极低,一般用 TTL 兜底即可,要求强一致就上锁或串行化。

### Q18:Redis 为什么快?你用过哪些数据结构?

**参考答案:**
纯内存操作、单线程命令处理避免上下文切换和锁竞争(6.0 后网络 IO 多线程,命令执行仍单线程)、IO 多路复用(epoll)、高效数据结构(SDS、跳表、压缩列表等)。

项目用到:String(图形验证码,`sessionId → 验证码`,5 分钟 TTL,验证后删除;缓存序列化后的对象)、Hash/ZSet 了解其场景(ZSet 可做排行榜,novel 的排行榜目前是 Caffeine + 定时重建,ZSet 是演进方向)。分布式锁底层是 String + Lua(Redisson 封装)。

---

## 五、novel 项目深挖(简历逐条对应)

### Q19:一分钟介绍一下 novel 这个项目和你的贡献。

**参考答案:**(准备 30 秒和 2 分钟两个版本,这是 2 分钟版)
novel 是一个前后端分离的小说平台,Spring Boot 3 + JDK 21 单体架构,分三个业务端:读者门户(推荐、搜索、阅读、评论、书架)、作家后台(发书、写作)、平台管理后台。它源自一个开源学习型项目,我在它基础上做二次开发:最大的增量是**独立完成了平台管理后台的全部后端**——管理员登录鉴权、运营数据看板、用户管理封禁、小说上下架、分类管理、评论审核、首页推荐位配置、作家/新闻/友链管理,一共 30 多个接口;过程中我把项目原有的鉴权策略体系扩展到管理端,并完整实践了多级缓存、分布式锁、限流、ES 搜索、MQ 数据同步这些工程能力。这个项目我还在持续迭代。

**追问 ⚠️:这是开源项目吧?哪些是你自己写的?**
(千万不要装作全是自己写的,面试官很可能认识这个项目)"对,基座是开源的 novel 学习型项目,我选它是因为想在一个规范的工程结构上做增量开发,这更接近入职后接手存量代码的真实场景。平台管理后台模块是我从表结构梳理、接口设计到实现独立完成的;门户和作家端我逐模块读过源码、能讲清每个组件的实现原理,并做过配置和细节修改。您可以任意挑管理后台的接口问实现细节。"——把"二次开发"转化成"能读懂存量代码 + 能做增量交付"两个卖点。

### Q20:管理后台的登录鉴权链路,从请求进来到 Controller,完整走一遍。

**参考答案:**
① 前端登录:`POST /api/admin/login`,后端校验用户名密码,查 `sys_user` 表,校验账号状态(禁用直接拒绝),通过后用 JJWT 生成 token——payload 的 subject 存管理员 id,header 里额外放了一个系统标识(admin),用 HMAC-SHA 对称签名。
② 后续请求带上 token,`AuthInterceptor` 拦截:从请求头取 token,从 URI 解析出系统前缀(admin),从 Spring 注入的 `Map<String, AuthStrategy>` 里取出 `adminAuthStrategy` 执行认证。
③ `AdminAuthStrategy` 里:JWT 验签 + 校验系统标识(防止拿门户端 token 访问管理端)、查库确认管理员存在且状态正常,然后把 id 放进 ThreadLocal(UserHolder)。
④ 认证失败抛业务异常,拦截器统一返回 JSON 错误;成功放行到 Controller,请求结束后 afterCompletion 清理 ThreadLocal。

**追问:为什么管理端策略里每次都要查库,门户端却走缓存?**
管理端对安全敏感度高、QPS 低,实时查库能保证"封禁立即生效";门户端 QPS 高,用户信息走缓存,封禁生效允许短暂延迟。这是安全性和性能的权衡。

### Q21 ⚠️:你的 JWT 怎么处理过期和注销?token 被盗了怎么办?

**参考答案:**(这是你代码里的真实坑:`JwtUtils.generateToken` 没有设置 exp,token 永久有效。必须提前想好答法)
诚实版:"我在复盘时发现基座项目的 JWT 工具类生成 token 时没有设置过期时间,这是个安全隐患,我的改进方案是:① 生成时加上 `exp` 声明(比如 2 小时),JJWT 解析时会自动校验过期;② 配合刷新机制——短效 access token + 长效 refresh token,或者滑动续期;③ 注销/踢人是 JWT 的天然短板,因为它无状态,服务端不存会话,方案是维护一个 Redis 黑名单(登出时把 token 的 jti 或用户 id + 签发时间写入,TTL 等于 token 剩余有效期),鉴权时先查黑名单;或者给每个用户存一个 token 版本号,改密码/封禁时版本号 +1,旧 token 全部失效。"

token 被盗:HTTPS 防传输截获、前端存储避免 XSS 可读(HttpOnly Cookie 或至少做好 XSS 防护——项目里有 XSS 过滤器)、敏感操作二次验证、异地登录风控。

**追问:JWT 和 Session 的取舍?**
Session 有状态,服务端可控(踢人容易),但分布式要做会话共享;JWT 无状态、天然适合多端和水平扩展,但吊销难、payload 只是 Base64 编码不能放敏感明文。项目选 JWT 是因为三个业务端 + 前后端分离,无状态更简单。

### Q22 ⚠️:注册的密码怎么存的?MD5 安全吗?

**参考答案:**(基座代码用 `DigestUtils.md5DigestAsHex` 且盐固定为 "0",这是可以主动"自曝 + 给方案"的点)
"基座实现是 MD5 直接摘要,并且盐字段是固定值,这不安全:MD5 计算太快,彩虹表和 GPU 暴力破解成本很低,固定盐等于没盐。我在 hmall 里用的是 Spring Security 的 BCryptPasswordEncoder,这才是正确做法:BCrypt 内置随机盐(每次加密结果都不同)、可调工作因子(算力增长时提高 cost),校验用 `matches()` 而不是比较密文。novel 这块我列入了改造计划,迁移方案是登录时先按旧算法校验,通过后用 BCrypt 重新加密落库,实现无感升级。"

### Q23:多级缓存里,一个请求查小说详情的完整链路?

**参考答案:**
Controller → Service → `BookInfoCacheManager.getBookInfo(id)`,方法标了 `@Cacheable(cacheManager = 本地缓存管理器, value = 缓存域名)`:① Caffeine 命中直接返回(纳秒级);② miss 则执行方法体查 MySQL(小说基础信息 + 首章 id),返回值自动写入缓存;③ 写场景(如管理端上下架、作家改书)调用 `@CacheEvict` 方法主动失效;④ 缓存域在枚举里统一声明容量和 TTL,Caffeine 用 maximumSize 控制内存、expireAfterWrite 控制时效。像"每个分类最近更新的 500 个小说 id"这类榜单,缓存 1 小时,牺牲一点实时性换 DB 压力大幅下降。

**追问:Caffeine 的淘汰策略是什么?**
Window TinyLFU:结合 LRU 的新近性和 LFU 的频率统计,用 Count-Min Sketch 近似记录访问频率,比纯 LRU 抗"偶发批量扫描"污染,命中率更高。

### Q24:Redisson 分布式锁,为什么不自己用 SETNX 写一个?你的 @Lock 注解切面怎么工作的?

**参考答案:**
自己写 SETNX + EXPIRE 有一堆坑:加锁和设过期不原子(要用 SET NX EX 一条命令)、业务超时锁提前过期导致并发进入、释放时可能误删别人的锁(要校验唯一值 + Lua 原子删除)、不可重入。Redisson 都解决了:锁值带客户端标识可重入,释放前校验;**看门狗机制**——不指定 leaseTime 时默认 30 秒,后台线程每 10 秒给还活着的持锁线程续期,避免业务没跑完锁先过期;底层 Lua 保证原子性;还支持公平锁、读写锁、RedLock。

切面流程:`@Around` 拦截标注 `@Lock` 的方法 → 反射拿到注解配置和参数上的 `@Key` → SpEL 解析出锁 key(比如"锁前缀::用户id",把锁粒度控制到单个用户而不是全局)→ `tryLock`(注解可配是否等待、等待多久)→ 拿到锁执行业务,finally 里 unlock;拿不到锁抛业务异常返回注解里配置的错误码。

**追问:Redis 主从切换时锁会失效吗?**
会。主节点写入锁后还没同步到从节点就宕机,从升主后锁丢了,另一个客户端能加锁成功。RedLock 是官方给的多数派方案但争议大;实际工程里一般接受这个极小概率,并在业务层加兜底(比如数据库唯一约束、状态机校验),关键是"锁 + 幂等"双保险。

### Q25:Sentinel 限流,匀速排队和快速失败有什么区别?为什么单 IP 限流用 ParamFlowRule?

**参考答案:**
项目配了两层:全局 QPS 上限走 `RATE_LIMITER`(匀速排队,漏桶思想)——超过阈值的请求不直接拒绝而是排队等待,适合削平突发毛刺、保护下游;而单 IP 的防刷规则用参数限流(ParamFlowRule),以第一个参数(IP)为维度分别计数,秒级 50、分钟级 1000,超了直接拒绝——刷子流量没有排队的意义。被限流的请求在拦截器里捕获 BlockException,返回统一的"请求过于频繁"错误码,而不是把异常抛给用户。

**追问:令牌桶和漏桶的区别?**
漏桶恒定速率流出,强行平滑,不允许突发;令牌桶按速率攒令牌、桶有容量,允许一定程度的突发流量。Sentinel 默认的快速失败基于滑动窗口计数,匀速排队是漏桶。

### Q26:ES 和数据库的数据一致性,你的增量 + 全量方案说说?为什么要"事务提交后再发消息"?

**参考答案:**
增量:小说信息变更(作家改书、管理端审核)后发一条消息到 RabbitMQ,消费者收到 bookId 后查库取最新数据写入 ES。全量:XXL-JOB 定时任务(每月一次)游标分批扫全表,用 ES bulk API 批量写入,作为增量链路丢消息、漏消息时的兜底对齐。

"事务提交后再发消息"解决的是:如果在事务内直接发 MQ,存在两种坏情况——① 消息发出去了,事务却回滚了,消费者会用"不存在的数据"更新 ES,产生脏数据;② 消息先被消费,查库时事务还没提交,读到旧数据。所以项目里封装了 `AmqpMsgManager`:检测到当前在事务中,就用 Spring 的 `TransactionSynchronizationManager` 注册一个 `afterCommit` 回调,把发消息推迟到事务成功提交之后;不在事务里就直接发。

**追问:afterCommit 里发消息失败了怎么办?**
这就是"本地事务 + 消息发送"无法双原子的问题:afterCommit 失败,DB 已提交但消息丢了。兜底:① 全量定时任务最终对齐(项目现状);② 更严格的方案是本地消息表——业务事务里同时插一条"待发送消息"记录,定时任务扫描重发,发送成功后标记,保证最终一致;③ RocketMQ 事务消息同理。能主动说出演进方案是加分项。

### Q27:为什么用 XXL-JOB 而不是 Spring 自带的 @Scheduled?

**参考答案:**
@Scheduled 是单机的:多实例部署会重复执行,没有统一管理。XXL-JOB 是调度中心 + 执行器架构:任务在控制台动态配置 cron、手动触发、看执行日志和失败告警;集群下有路由策略(轮询、分片、故障转移),天然避免重复执行,还支持分片广播加速大数据量任务。全量同步 ES 这种任务需要可视化管理和失败重试,用调度平台更合适。

### Q28:ShardingSphere 分库分表,为什么是 book_content 这张表?分片键怎么选?

**参考答案:**
小说正文表是全库增长最快的大表:一本书几千章、每章一条大文本,行数和空间都远超其他表,单表过大后 B+ 树层高增加、索引维护变慢。项目里用 ShardingSphere-JDBC(客户端分片,无独立代理层)按章节 id 哈希把 `book_content` 水平拆成 10 张表。分片键选章节 id 是因为正文的读写都是"按章节 id 精确查询",路由能直接命中单表;不用 book_id 是避免热门书数据倾斜到一张表。代价:跨分片的聚合、排序、join 受限,但正文表没有这类查询,业务上是干净的分片场景。

**追问:分表后 id 怎么生成?**
不能再用单表自增。方案:雪花算法(时间戳+机器位+序列,趋势递增,注意时钟回拨)、号段模式(Leaf)、或数据库全局序列。

### Q29:Spring AI 那块做了什么?调用大模型要注意什么?

**参考答案:**
作家后台的 AI 写作辅助:扩写、缩写、续写、润色四个接口,用 Spring AI 的 ChatClient 调 OpenAI 兼容接口(硅基流动),把用户文本包进 prompt 模板。工程注意点:① 超时——大模型响应慢,项目里自定义了 RestClient 的连接超时 5s、读超时 60s,避免默认配置下请求hang死容器线程;② 项目全局配置过"数字序列化成字符串",会把 temperature: 0.7 序列化成字符串导致模型服务报参数错误,所以单独配置了 ChatClient 用的 RestClient 消息转换,这是真实踩过的坑;③ 演进方向:同步阻塞改 SSE 流式输出提升体验、加频控和内容审核、API key 放配置中心而不是明文(⚠️ 如果被问 key 管理,主动承认配置文件明文是错误示范,应环境变量/配置中心 + 定期轮换)。

### Q30:XSS 和 SQL 注入,项目里分别怎么防的?

**参考答案:**
XSS:自定义 Filter + HttpServletRequestWrapper 装饰器,对请求参数做 HTML 转义/清洗,把 `<script>` 这类内容在入口处净化,评论、昵称等用户输入场景防止存储型 XSS。
SQL 注入:MyBatis 一律 `#{}` 预编译占位,杜绝 `${}` 拼接;但"排序字段"这种没法预编译的场景(ORDER BY 后面不能放占位符),项目里做了 AOP + 白名单校验——`@ValidateSortOrder` 注解 + 切面,把前端传来的 sort 字段和白名单比对,不在白名单直接拒绝,防止 `ORDER BY (CASE WHEN ...)` 型注入。

---

## 六、hmall 项目深挖

### Q31:为什么要微服务拆分?拆分带来了哪些新问题,你们怎么解决的?

**参考答案:**
拆分依据是业务边界(DDD 的限界上下文思想的朴素版):商品、购物车、用户、交易、支付各自独立成服务,好处是独立开发部署、故障隔离、按需扩容(大促时单独扩交易服务)。

但每个好处都有代价,这套项目基本把代价踩了个遍:① 服务间调用——OpenFeign + Nacos 服务发现;② 统一入口和鉴权——Gateway;③ 用户上下文跨服务丢失——网关解析后透传 header,公共拦截器还原;④ 本地事务失效——Seata 分布式事务;⑤ 强耦合的同步调用——RabbitMQ 异步化;⑥ 配置分散——Nacos 配置中心共享配置。面试时能把"问题 → 组件"的映射讲清楚,比背组件名有说服力得多。

### Q32:网关鉴权的完整流程?下游服务凭什么信任 user-info 这个头?

**参考答案:**
`AuthGlobalFilter` 实现 GlobalFilter + Ordered(order=0 保证先执行):① 用 AntPathMatcher 匹配白名单(登录、商品搜索等公开路径)直接放行;② 其他请求从 authorization 头取 token,用 JwtTool 验签(RSA 非对称,私钥在 jks 密钥库,网关验签即可);③ 失败返回 401 终止;④ 成功则 `exchange.mutate()` 把 userId 写进 `user-info` 请求头传给下游。下游服务通过 hm-common 里自动装配的 MVC 拦截器读这个头,写入 UserContext(ThreadLocal),业务代码 `UserContext.getUser()` 直接用。

⚠️ **追问:我伪造一个 user-info 头直接打你下游服务,是不是就冒充任何人了?**
是的,这是当前架构的信任前提:微服务只在内网,外部流量必须走网关,网关会覆盖请求头。诚实回答现状,然后给加固方案:① 网络层隔离,下游服务不暴露公网(K8s 里用 NetworkPolicy);② 网关给透传信息加签名(HMAC),下游校验;③ 服务间 mTLS/零信任。能说出"边界在哪、怎么加固"就到位了。

**追问:Gateway 的过滤器和 MVC 拦截器什么区别?**
Gateway 基于 WebFlux 响应式,过滤器是异步非阻塞的 GlobalFilter/GatewayFilter,不能用 Servlet API;MVC 拦截器是同步阻塞模型。所以 hm-common 里的 MVC 配置要做条件装配,避免在网关(WebFlux 环境)里生效报错。

### Q33:动态路由是怎么实现的?为什么要做这个?

**参考答案:**
默认路由写在配置文件里,改路由要重启网关——网关是全站入口,重启有风险。我的实现:网关启动时通过 NacosConfigManager 用 `getConfigAndSignListener` 拉取路由配置(JSON 格式的 dataId)并注册监听器,首次拉到的配置立即解析生效;之后运维在 Nacos 控制台改路由,监听器回调触发:先按记录的 routeId 删除旧路由表,再把新的 RouteDefinition 逐条写入 RouteDefinitionWriter,全程不重启、秒级生效。细节:必须记录已发布的 routeId 集合,否则删不干净会有幽灵路由;写入后要 subscribe 触发(响应式 API 不订阅不执行)。

### Q34:hm-common 的自动装配怎么做的?为什么不用 @ComponentScan 扫进来?

**参考答案:**
公共模块的配置类(MVC 拦截器注册、MyBatis-Plus 分页插件、MQ 消息转换器、Jackson 配置)写在 `META-INF/spring.factories` 里声明为 `EnableAutoConfiguration` 条目,各服务只要引依赖就自动生效。不用 @ComponentScan 是因为:公共包名和业务包名不同,要求每个服务改扫描路径侵入性强、容易漏;自动装配还能配合 `@ConditionalOnClass` 等条件注解做到"环境不满足就不装配"——比如 MVC 配置只在 Servlet 环境生效,网关(WebFlux)引了 common 也不会报错。追问新版本:Spring Boot 2.7+ 推荐 `META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports` 文件,3.0 移除了 spring.factories 的自动配置支持,原理一致。

### Q35:OpenFeign 的工作原理?你们的容错怎么做的?

**参考答案:**
原理:接口上标 `@FeignClient`,启动时扫描生成 JDK 动态代理;调用时代理把方法参数编码成 HTTP 请求,从 Nacos 拿服务实例列表,负载均衡(Spring Cloud LoadBalancer)选一个发请求,响应解码成返回对象。本质是"把 HTTP 调用伪装成本地方法调用"。

项目里的容错三件套:① RequestInterceptor 统一把 UserContext 的 userId 塞进请求头,解决跨服务用户上下文丢失(这是必须的,否则下游拿不到"谁在操作");② FallbackFactory 降级——比 Fallback 多拿到异常原因可以记日志;策略上区分读写:查商品失败返回空集合(购物车页面降级展示,不至于整页挂掉),扣库存失败必须抛异常(下单不允许"假成功");③ 结合 Sentinel 做熔断,依赖服务持续故障时快速失败,防止线程被拖死产生雪崩。另外开了 Feign 日志(FULL 级别)方便联调排查。

**追问:Feign 的超时怎么配?默认多少?**
connectTimeout 和 readTimeout,默认都比较长(旧版 10s/60s 级别),生产必须显式调小(比如 1s/3s),否则下游变慢时上游线程大量堆积——超时设置是防雪崩的第一道闸。

### Q36:Seata AT 模式的原理?为什么选 AT?它有什么坑?

**参考答案:**
下单要跨交易、购物车、商品三个服务(建单、清购物车、扣库存),本地事务管不了。AT 模式两阶段:一阶段,各分支执行业务 SQL 前后,Seata 数据源代理解析 SQL 自动生成前后镜像存入 `undo_log`,和业务在同一个本地事务提交,并向 TC(Seata Server)注册分支、拿全局锁;二阶段,全局提交只需异步删 undo_log(很快),全局回滚则用 undo_log 反向补偿生成回滚 SQL。对业务代码的侵入只有入口方法一个 `@GlobalTransactional` 注解。

选 AT:业务简单、不想写 TCC 的 Try/Confirm/Cancel 三段代码,AT 无侵入开发效率高。坑/局限:① 性能——全局锁按"表+主键"粒度,热点行(比如同一件爆款商品的库存行)高并发下单会串行化,吞吐骤降;② 默认读未提交的全局隔离级别,一阶段提交的数据可能被全局回滚,期间别的事务读到了就是"脏读",需要 `@GlobalLock` 或 select for update 增强;③ 回滚依赖后镜像校验,如果数据被绕过 Seata 改过,回滚会失败进入人工处理。高并发扣库存场景更常用"MQ 最终一致 + 库存服务内本地事务"替代强一致。

### Q37 ⚠️:扣库存怎么防超卖?(你的代码目前防不了,必考必挖)

**参考答案:**
(你 ItemMapper 的 SQL 是 `UPDATE item SET stock = stock - #{num} WHERE id = #{itemId}`,没有库存条件——诚实自曝 + 完整方案,反而变成亮点)
"我复盘时发现自己最初写的扣减 SQL 只有原子递减、没有条件保护,并发下库存会被扣成负数,这是超卖。正确做法分层:
① 数据库层兜底:`UPDATE item SET stock = stock - #{num} WHERE id = #{itemId} AND stock >= #{num}`,WHERE 里带库存充足条件,利用行锁 + 条件更新保证原子性,然后**必须检查影响行数**,为 0 说明库存不足,抛异常触发整个下单事务回滚——条件更新不校验返回值等于没写;
② 应用层优化:高并发场景先在 Redis 预扣(Lua 脚本保证'查+扣'原子),挡掉大部分无效流量,DB 只承接预扣成功的请求,异步或同步落库;
③ 极端热点(秒杀):请求先进 MQ 排队串行化,或按商品分桶库存降低单行竞争。
另外批量扣减时部分成功部分失败要整体回滚,我的实现里 executeBatch 在一个事务里,配合条件更新的行数校验就是完整方案。"

### Q38:支付这块,幂等是怎么做的?为什么要做幂等?

**参考答案:**
必要性:用户重复点击、前端重试、网络超时后的重发,都会造成同一笔业务被提交多次;支付场景重复执行就是资损。

项目里两层幂等:① 创建支付单:按业务订单号查已有支付单——不存在才新建;已存在且已支付,拒绝并提示;已存在未支付但金额等要素一致,直接复用返回,不产生第二笔。② 支付成功标记:UPDATE 带状态前置条件(status 必须是未支付),影响行数为 0 说明已被处理过,直接失败——天然幂等,重复回调不会二次改状态。通用幂等思路还有:唯一索引(业务单号唯一)、分布式锁、幂等表(token 机制)。

### Q39:支付成功为什么用 MQ 通知订单服务,而不是直接 Feign 调用?

**参考答案:**
最初就是 Feign 同步调用,问题:① 耦合——支付服务必须知道"谁关心支付结果",以后积分、优惠券、物流都要通知,支付代码越改越臃肿;② 可用性传导——订单服务挂了,支付主流程跟着失败,但"扣款成功"这个事实已经发生,不该被下游拖累;③ 性能——同步等待拉长支付接口 RT。改成发布订阅:支付成功后向 `pay.direct` 交换机发消息(路由键 pay.success),订单服务自己声明队列绑定监听,标记订单已支付;将来加订阅方支付服务零改动。发送包在 try-catch 里,MQ 不可用时记录错误日志但不阻塞支付返回——支付的成功不依赖通知的成功,后续靠补偿对齐。

**追问 ⚠️:你这个监听器代码里,消息消费的判断条件写的是 `if (order != null || order.getStatus() != 1) return;`,有什么问题?**
(这是你代码里的真实 bug,面试官若看到会直接问;就算不问,你也可以主动当"我修过的 bug"讲)
两个错:逻辑或应为逻辑与的语义——本意是"订单不存在或状态不是未支付就跳过",应写 `order == null || order.getStatus() != 1`;当前写法在 order 为 null 时会短路进第二个条件直接空指针,order 不为 null 时第一个条件为真直接 return,导致**所有消息都被跳过,订单永远不会被标记为已支付**。这个 bug 的教训是:消费者逻辑必须有单元测试和联调验证,不能只测发送方;判空永远放在使用之前。

---

## 七、实际开发场景题(考设计能力)

### Q40:让你在 novel 里新增"小说打赏"功能,说说接口和表怎么设计?

**参考答案:**(考察点:能不能像工程师一样思考,而不是只会 CRUD)
先问清需求边界:打赏用什么支付(余额/直连支付)、要不要分成给作家、要不要排行榜。假设用账户余额打赏、作家按比例分成:

表设计:打赏流水表 `reward_log`(id、user_id、book_id、author_id、金额、分成金额、状态、幂等号 request_id 唯一索引、时间),余额变动复用用户消费流水;作家收入落 `author_income_detail`。
接口:`POST /api/front/book/reward`,入参 bookId、金额、前端生成的 requestId;鉴权走既有 JWT 链路。
核心逻辑的工程考量:① 幂等——requestId 唯一索引,重复提交插入冲突直接返回原结果;② 原子性——扣余额用条件更新 `balance >= amount` 并校验影响行数,和写流水在同一个本地事务;③ 并发——同一用户加 Redisson 锁或依赖余额条件更新兜底;④ 异步——打赏成功后发 MQ,异步更新作家收入、排行榜、通知,主链路只保"扣钱 + 流水";⑤ 对账——流水表是唯一事实来源,定时核对余额变动与流水汇总。
能主动讲出幂等、原子扣减、异步化、对账四件事,这题就过了。

### Q41:接口幂等有哪些通用方案?分别适合什么场景?

**参考答案:**
① 数据库唯一索引:业务天然有唯一号(订单号、支付单号)时首选,插入冲突即重复,简单可靠;
② 状态机 + 条件更新:有状态流转的业务(支付单从未支付→已支付),UPDATE 带状态前置条件,影响行数为 0 即重复——hmall 支付就是这个;
③ 幂等 token:前端进页面先领 token,提交时带上,服务端 Redis 校验并删除(Lua 保证原子),适合防表单重复提交;
④ 分布式锁:锁住业务键短暂串行化,适合逻辑复杂无法用上面三种的;
⑤ 消费端幂等:MQ 消息带唯一 id,消费前查处理记录表/Redis。
原则:能用数据库约束就不用应用层技巧,越靠近数据的防线越可靠。

### Q42:一个列表接口,产品要求支持按多个字段排序、多个条件筛选,你怎么防止它变成慢查询?

**参考答案:**
① 枚举化排序字段,白名单校验(novel 里有现成实践),同时避免 SQL 注入和"对无索引字段排序";② 筛选组合爆炸时,不可能每种组合都建索引——分析高频组合建 2~3 个联合索引(区分度高的列在前,排序字段放最后),低频组合接受走次优索引;③ 限制最大页深和 pageSize;④ 复杂多维筛选 + 全文搜索的组合,超过 MySQL 舒适区就引入 ES(novel 的书籍搜索就是这么演进的);⑤ 上线后用慢查询日志验证,而不是拍脑袋。

### Q43:你在项目里是怎么用 Git 协作的?代码评审你会看什么?

**参考答案:**
分支模型:feature 分支开发,提交信息写清"动词 + 范围 + 目的";合并前 rebase 保持线性历史(或按团队规范 merge);冲突本地解决并回归测试。评审关注(按优先级):正确性(边界、判空、并发、事务范围)> 安全(注入、越权、敏感信息)> 可读性(命名、函数长度)> 性能(循环里查库、N+1)。我自己踩过消费者判空顺序的 bug(hmall 监听器那个),所以评审时对"判空时机、||/&& 逻辑"这类低级但致命的问题特别敏感。

---

## 八、线上服务与故障排查(场景题,重点准备)

> 面试官考这类题,看的是**方法论**:先止血恢复服务,再定位根因,最后复盘预防。回答时按"现象确认 → 快速止血 → 定位 → 根因 → 修复 → 预防"的骨架讲,哪怕细节不全,思路分也能拿满。实习生不要求全都亲历过,但要能讲清楚"我会怎么做"。

### Q44:线上接口突然大面积超时,CPU 飙到 100%,你怎么排查?

**参考答案:**
止血:如果有多实例,先把异常实例摘流量(注册中心下线),保留现场不要急着重启;单实例评估重启止血 vs 保留现场的取舍(可以先抓完诊断信息再重启)。
定位(Linux + JVM 三板斧):
① `top` 确认是哪个进程;`top -Hp <pid>` 找出 CPU 最高的线程,把线程号转 16 进制;
② `jstack <pid>` 导出线程栈,搜索该 16 进制 nid,看它卡在哪段业务代码;有 Arthas 的话 `thread -n 3` 一步到位;
③ 常见根因:死循环(比如 while 条件写错)、正则回溯灾难、频繁 Full GC(GC 线程占满 CPU——`jstat -gcutil` 看 GC 频率,老年代占用居高不下基本可断定)、大量线程自旋。
修复与预防:改代码;上线前压测;接入 CPU/GC 监控告警(Prometheus + Grafana 或 Spring Boot Admin——novel 里接过,可以主动提)。

### Q45:服务 OOM 挂了,怎么定位是哪里泄漏的?

**参考答案:**
事前保障:JVM 参数加 `-XX:+HeapDumpOnOutOfMemoryError -XX:HeapDumpPath=...`,OOM 时自动留 dump;没配的话用 `jmap -dump` 抓(会 STW,谨慎)。
分析:MAT / JProfiler 打开 dump,看 Dominator Tree 找占用最大的对象,顺着 GC Roots 引用链找到"谁持有了不该持有的引用"。
典型案例要能随口举:① ThreadLocal 用完不 remove,线程池里 value 累积——我项目里拦截器 afterCompletion 统一清理就是防这个;② 无界集合当缓存,只进不出——本地缓存必须用 Caffeine 这种带容量上限和淘汰策略的,不能裸用 HashMap;③ 一次性查全表大结果集(比如导出没分页);④ 流/连接没关。
预防:代码规范 + 压测 + 堆内存告警(用量持续爬坡不回落就是泄漏信号)。

### Q46:凌晨 Redis 宕机了 10 分钟,你的 novel 系统会发生什么?怎么让它更健壮?

**参考答案:**
影响面推演(考察对自己系统的理解):① 走 Redis 的缓存读全部异常——Spring Cache 默认缓存操作异常会直接抛出,接口报错;② 图形验证码读写失败,注册登录不可用;③ Redisson 加锁失败,标了 @Lock 的业务全部拒绝;④ Caffeine 本地缓存不受影响,命中本地缓存的读请求还活着——这正是多级缓存的容灾价值。
加固:① 自定义 CacheErrorHandler:缓存 get/put 异常降级为"当作 miss,直接查库",让 Redis 故障退化成性能问题而不是可用性问题(注意评估 DB 能不能扛住裸奔流量,配合限流);② Redis 高可用:主从 + 哨兵自动故障转移;③ 分布式锁场景区分"锁是保正确性还是保性能":保性能的锁失败可放行,保正确性的(如防重复支付)必须失败并给出友好提示;④ 演练:故意 kill Redis 验证降级链路,故障预案要演练过才算数。

### Q47:某天一本小说上了热搜,详情接口 QPS 暴涨,缓存正好过期,数据库被打挂了——复盘一下这个故障。

**参考答案:**(缓存击穿的完整故障叙事,把它讲成"我理解的事故复盘")
现象:DB 连接数打满、CPU 飙高,详情接口大量超时,其他依赖同库的接口被连坐。
止血:对该接口紧急限流(Sentinel 上调不了就网关层限);手动把热点数据塞回缓存;DB 重启前先掐掉洪峰来源,否则起来又被打挂。
根因:热点 key 过期瞬间,成百上千并发同时 miss、同时回源,DB 瞬时压力超限——缓存击穿;连坐效应是因为没有连接池隔离。
修复与预防:① 互斥重建:miss 后先抢分布式锁(项目里现成的 Redisson),抢到的查库回填,没抢到的短暂等待后重读缓存——DB 只承受 1 个回源请求;② 热点数据逻辑过期:物理不过期,值里带逻辑过期时间,过期后异步重建,读方永远有旧值可用;③ 热点探测 + 永不过期白名单;④ 兜底永远要有:Sentinel 对 DB 访问链路限流,保证最坏情况下"部分请求失败"而不是"全站雪崩"。novel 里 Caffeine 在 Redis 之前还有一层,也天然削掉了大部分击穿流量。

### Q48:RabbitMQ 消息堆积了 50 万条,消费越来越慢,怎么办?

**参考答案:**
先判断堆积原因,分两类处理:
消费者出问题(最常见):看消费者是否存活、是否在反复报错重回队列(死循环重试是堆积加速器)。止血:修复消费者;临时扩容消费者实例数、调大并发消费者数和 prefetch;有毒消息(反复消费失败的)设置重试上限后进死信队列隔离,别让一条坏消息卡死整个队列。
生产速率天然高于消费速率:优化消费逻辑(批量处理、去掉消费链路里的慢操作如逐条查库改批量)、上分片/多队列并行。
极端情况:堆积到 MQ 磁盘告警,先把消息快速搬运到临时存储(写库/写文件)腾空队列,再离线慢慢补偿处理。
预防:队列长度监控告警、消费 lag 监控、消费者幂等(扩容多消费必然带来重复投递)。结合项目:ES 同步消息堆积的后果是搜索数据滞后,属于可容忍降级,处理优先级可以排在支付通知这类资金消息之后——能讲出"不同消息的堆积后果不同,处置优先级不同"是加分项。

### Q49:用户支付成功了,但订单一直显示未支付,用户投诉了——完整排查这条链路。

**参考答案:**(把 hmall 的支付-MQ-订单链路变成故障题,大概率被问)
排查顺序沿数据流走:
① 查支付服务:支付单状态是否已成功?——是,说明扣款和状态标记没问题,问题在通知链路;
② 查发送端:支付服务日志里有没有"发送 MQ 失败"的 error(代码里 catch 后记日志)?RabbitMQ 管理台看 pay.direct 交换机有没有消息进来、有没有路由到队列(没绑定的路由键消息会被丢弃——可配 return callback 捕获);
③ 查队列:trade.pay.success.queue 是否有消息堆积(消费者挂了)?还是消息进了队列又被 ack 掉了(消费了但业务没生效)?
④ 查消费端:订单服务日志——本项目里真实原因就是消费者判空逻辑 bug,所有消息被静默跳过(见 Q39);
⑤ 修复数据:补偿脚本按支付单反查订单批量修正状态。
预防(重点):① 关键链路加对账任务——定时扫"支付成功但订单未支付"的不一致数据,自动补偿或告警,这是资金链路的标配兜底;② 消息可靠性三段:生产者 confirm 机制 + 失败落表重发,队列和消息持久化,消费者手动 ack + 幂等;③ 消费逻辑必须有日志,"静默跳过"是排查噩梦。

### Q50:接口平时 50ms,每天固定某个时段飙到 2 秒,怎么排查?

**参考答案:**
"固定时段"是最大线索,大概率是周期性任务或周期性流量:① 对照时间点查:定时任务(XXL-JOB 控制台执行记录)、数据库备份/大批量 job、竞对爬虫高峰、缓存集中过期(TTL 设成整点对齐会造成整点回源洪峰——TTL 要加随机抖动);② APM/日志拆解耗时:慢在 DB(连接池等待还是慢 SQL)、Redis、还是外部调用;③ DB 侧:慢查询日志按时段过滤,看是否有大事务/锁等待(`SHOW ENGINE INNODB STATUS`、锁等待视图);④ JVM 侧:该时段 GC 日志有没有 Full GC 尖刺(大任务制造大量对象)。
典型答案:全量同步任务和高峰期撞车,把任务错峰、限速(游标批次间 sleep)、或读库跑任务。展示"用时间相关性缩小范围"的思路是关键。

### Q51:数据库连接池(Hikari)耗尽,大量请求报"connection timeout",什么原因?

**参考答案:**
本质:连接被借走后长时间不还。排查:① Hikari 开 leakDetectionThreshold(如 60s)定位借走不还的代码栈;② 常见根因:慢 SQL 持连接(EXPLAIN 优化)、大事务(事务里做 RPC/发 MQ 等慢操作——事务要短,novel 里"事务提交后再发消息"也顺带缩短了事务)、锁等待串联(一个大 UPDATE 锁住热点行,后续全排队)、连接池配置过小而下游变慢放大了占用时间;③ 止血:重启前先抓 `SHOW PROCESSLIST` 看连接都在干什么。
预防:事务范围最小化、SQL 上线前 review、连接池使用率监控告警、接口超时上限 < 用户耐心值。

### Q52:网关偶发 401,用户 token 明明没问题,可能是什么原因?

**参考答案:**
"偶发"指向环境差异而不是代码逻辑:① 多实例配置不一致——某台网关的验签公钥/密钥库版本旧(发布不彻底),打到它就 401:对比各实例配置版本,看 401 是否集中在某实例;② 时钟偏移——签发方和校验方服务器时间差超过 token 有效期余量,NTP 校准;③ 白名单模式匹配问题——AntPathMatcher 的 pattern 写法在部分路径下不匹配(`/api/**` vs `/api/*` 的区别);④ header 大小写/多值问题——某些代理会改写 authorization 头;⑤ token 在网关被截断(header 超长限制)。
方法论:偶发问题先加结构化日志(打出实例 id、失败原因细分:过期/验签失败/格式错),把"偶发"变成"可统计",再看规律。

### Q53:发布新版本后,Nacos 上服务是 UP 的,但调用方一直报 no available instance / 调用旧逻辑,怎么排查?

**参考答案:**
① 命名空间/分组不一致:调用方和提供方注册到了不同 namespace/group,控制台确认双方的注册详情;② 本地缓存:服务发现有本地缓存和拉取间隔,刚上线的实例调用方还没感知,等待或触发刷新;③ 实例元数据:健康检查失败(心跳超时)导致实例被标记不健康但控制台粗看是绿的;④ "调用旧逻辑":滚动发布时新旧实例并存,负载均衡轮询会打到旧实例——确认旧实例是否真的下线(优雅下线:先从注册中心摘除、等流量排空再 kill,否则会有一波调用打到已死实例上报错);⑤ 网络层:容器环境里注册的 IP 是内部 IP,调用方不可达。
这题考微服务的"注册-发现-负载均衡"全链路心智模型,答的时候沿链路逐段怀疑。

### Q54:线上发现 item 表有商品库存是负数(超卖已经发生了),你作为值班同学怎么处理?

**参考答案:**(事故处理题,考冷静度和优先级)
① 止血优先:受影响商品紧急下架或置为不可售,停止扩大损失;保留现场(负库存记录、相关订单)截图/备份;
② 影响评估:多少 SKU、多少订单超卖,拉出"库存变动流水 vs 订单明细"对不上的清单——量化影响是上报和决策的前提;
③ 数据修复:和业务方确认策略——已支付的超卖订单是补货、退款还是换购;库存数据以"实际库存盘点 + 订单占用"重算修正,修数脚本先在预发验证、留回滚方案;
④ 根因:扣减 SQL 缺少 `stock >= num` 条件(我项目里真实存在过这个问题),并发下多个事务同时通过了应用层校验、各自扣减成功;
⑤ 修复:条件更新 + 影响行数校验 + 抛异常回滚(见 Q37);
⑥ 预防:上线前并发用例(JMeter 对同一商品并发下单)必测;加"库存不为负"的对账巡检任务,异常告警——**同类问题要靠机制拦住,而不是靠人小心**。

### Q55:ES 搜索结果和数据库对不上(新书搜不到/下架的书还在),怎么系统性排查和治理?

**参考答案:**
排查:① 确认增量链路:变更时消息发了吗(生产日志)→ MQ 路由到了吗(管理台)→ 消费者消费成功了吗(消费日志/异常)——三段逐一验证;② 常见断点:事务回滚但消息已发(项目里用 afterCommit 已规避)、消费异常后消息被丢弃没有重试、ES 写入版本冲突;③ 确认全量兜底:XXL-JOB 上次执行是否成功。
治理:① 消费失败重试 + 死信告警;② 全量任务提频或增加"增量对账"任务(比对最近 N 天变更记录与 ES 文档的 update_time);③ 删除场景要显式处理——下架/删除也要发消息删 ES 文档,全量同步只做 upsert 的话,已删数据永远清不掉(可以用"全量重建到新索引 + 别名切换"解决存量脏数据);④ 接受最终一致的定位:搜索允许秒级延迟,但要有能观测的"延迟指标",超阈值告警。

---

## 九、简历防挖坑清单 + 反问环节

### 面试官一定会挖的点,提前备好口径

| 简历表述 | 面试官的挖法 | 应对要点 |
|---|---|---|
| 开源项目二次开发 | "哪些是你写的?" | 大方承认基座开源,管理后台 30+ 接口是独立完成,欢迎追问细节(Q19) |
| JWT 鉴权 | "过期?注销?被盗?" | 主动说基座无 exp 的缺陷 + 三层改进方案(Q21) |
| Elasticsearch / RabbitMQ / XXL-JOB | "线上真的开着吗?规模多大?" | 诚实:学习项目,组件按可开关设计(@ConditionalOnProperty 条件装配,这本身是个亮点),本地/云上都完整跑通过链路;数据量是教学级,但链路设计考虑了生产级问题(丢消息、事务时序) |
| Seata 分布式事务 | "AT 的全局锁?脏读?" | Q36 的坑必须能讲,只会用不懂原理是减分项 |
| 防超卖 | "你的 SQL 真防得住吗?" | 自曝旧版缺陷 + 条件更新/预扣/排队三层方案(Q37) |
| 缓存一致性 | "先删还是先更?本地缓存多实例?" | Q15/Q17 |
| "至今持续迭代" | "最近在迭代什么?" | 准备 1-2 个真实在做/计划做的点:如 JWT 过期改造、密码 BCrypt 迁移、库存条件更新修复——正好都是本文档里的改进项,做完就是真话 |

### 建议的反问(体现判断力,别问薪资加班)

1. "实习生进来一般接什么类型的任务?会有 mentor 做 code review 吗?"——表达对成长路径的关注。
2. "团队目前的技术栈和我项目里用的这套(Spring Cloud Alibaba / MQ / Redis)重合度怎么样?入职前我可以提前补什么?"——表达即战力意愿。
3. "团队怎么做线上问题的复盘?实习生能参与吗?"——呼应你故障排查的准备,面试官会记住这个问题。

---

## 附:高频追问链速查(面试前 30 分钟扫一遍)

- HashMap → 线程不安全 → ConcurrentHashMap → 1.7/1.8 区别
- ThreadLocal → 内存泄漏 → 为什么 key 弱引用 → 线程池串号(项目实例)
- 索引 → B+ 树 → 回表/覆盖索引 → 最左前缀 → 索引失效场景 → EXPLAIN
- 事务 → 隔离级别 → MVCC → 间隙锁 → 大事务危害(项目:事务后发消息)
- Redis → 缓存三兄弟 → 一致性 → 分布式锁 → 看门狗 → 主从切换锁失效
- JWT → 结构 → 过期/注销(⚠️ 项目坑)→ 和 Session 取舍
- MQ → 为什么用 → 丢消息三段保障 → 重复消费幂等 → 堆积处理 → 死信/延迟
- 微服务 → 拆分依据 → 网关鉴权 → Feign 原理/超时 → 熔断降级 → Seata AT → 超卖(⚠️ 项目坑)
- 故障题万能骨架:现象确认 → 止血 → 定位(日志/监控/工具)→ 根因 → 修复 → 复盘预防


