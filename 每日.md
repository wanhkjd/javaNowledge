



# Spring 面试题及答案

> 参考：[[小林coding/Spring面试题.md]]

## 1. 你对 Spring 了解多吗？Spring 里面的核心是什么？

Spring 是一个轻量级的 Java 企业级开发框架，主要用于管理对象、解耦组件，并为 Web 开发、事务管理、数据访问等提供统一的解决方案。

Spring 的核心主要是 **IoC、DI 和 AOP**：

- **IoC（控制反转）**：把对象的创建、依赖关系和生命周期交给 Spring 容器管理，不再由业务代码主动 `new` 对象。
- **DI（依赖注入）**：IoC 的具体实现方式。Spring 在创建 Bean 时，将它所依赖的对象注入进来。
- **AOP（面向切面编程）**：把日志、事务、权限校验等横切逻辑从业务代码中抽离，通过代理统一增强目标方法。

除此之外，Spring 还提供了事务管理、Spring MVC、事件机制等功能。总体来说，Spring 的核心目标是实现**高内聚、低耦合**，提高代码的可维护性和可扩展性。

---

## 2. 简单讲解一下 IoC 跟 DI

### IoC

IoC 是 **Inversion of Control，控制反转**。

传统开发中，一个对象需要依赖另一个对象时，通常会在类内部通过 `new` 主动创建。使用 Spring 后，对象的创建权和依赖关系的管理权交给了 IoC 容器，因此称为“控制反转”。

```java
// 传统方式：对象自己创建依赖
private UserRepository userRepository = new UserRepositoryImpl();
```

### DI

DI 是 **Dependency Injection，依赖注入**，是实现 IoC 的主要方式。

Bean 只声明自己需要哪些依赖，Spring 容器创建好依赖对象后，再把它们注入 Bean。常见方式有：

1. **构造器注入**：依赖关系明确，可以保证对象创建完成时依赖已经就绪，通常最推荐。
2. **Setter 注入**：适合可选依赖，但对象可能出现依赖尚未设置完整的情况。
3. **字段注入**：使用方便，但会隐藏依赖关系，也不利于单元测试，一般不推荐在生产代码中使用。

```java
@Service
public class UserService {
    private final UserRepository userRepository;

    public UserService(UserRepository userRepository) {
        this.userRepository = userRepository;
    }
}
```

一句话总结：**IoC 是把对象的控制权交给容器的思想，DI 是容器把依赖注入对象的实现手段。**

---

## 3. Spring 跟 Spring Boot 之间的区别是什么？

Spring 是基础框架，提供 IoC、AOP、事务管理、Spring MVC 等核心能力；Spring Boot 构建在 Spring 之上，主要用于简化 Spring 应用的配置、启动和部署。

| 对比项 | Spring | Spring Boot |
| --- | --- | --- |
| 定位 | Java 企业级开发基础框架 | 快速构建 Spring 应用的脚手架和整合方案 |
| 配置 | 通常需要开发者进行较多配置 | 自动配置，强调“约定大于配置” |
| 依赖管理 | 需要自行选择、组合和管理依赖 | 通过 Starter 整合常用依赖 |
| Web 容器 | 可以部署到外部 Servlet 容器 | 默认支持内嵌 Tomcat、Jetty、Undertow |
| 部署方式 | 常见方式是打成 WAR 部署 | 通常打成可执行 JAR，直接运行 |

Spring Boot 并不是 Spring 的替代品，也没有改变 Spring 的核心原理，而是通过 **Starter、自动配置、内嵌服务器和默认约定**，降低 Spring 项目的配置与部署成本。

---

## 4. Bean 的作用域有哪些？

Spring 常见的 Bean 作用域如下：

- **singleton**：默认作用域。在一个 Spring IoC 容器中，同一个 Bean 通常只有一个实例。
- **prototype**：每次从容器获取 Bean 时，都会创建一个新实例。
- **request**：每个 HTTP 请求创建一个实例，只适用于 Web 环境。
- **session**：每个 HTTP Session 创建一个实例，只适用于 Web 环境。
- **application**：每个 `ServletContext` 中创建一个实例，只适用于 Web 环境。
- **websocket**：每个 WebSocket 会话创建一个实例。
- **自定义作用域**：可以通过实现 Spring 的 `Scope` 接口进行扩展。

可以使用 `@Scope` 指定作用域：

```java
@Bean
@Scope("prototype")
public UserService userService() {
    return new UserService();
}
```

需要注意：Spring 会完整管理 singleton Bean 的生命周期；对于 prototype Bean，Spring 负责创建和初始化，但将其交给使用者后，通常不再负责销毁。

---

## 5. Spring MVC 中的 Bean 是单例的吗？

**默认是单例，但不是强制只能是单例。**

Spring MVC 中的 `Controller`、`Service`、`Repository` 等组件，本质上都是由 Spring IoC 容器管理的 Bean。由于 Spring Bean 的默认作用域是 `singleton`，所以这些 Bean 默认也都是单例的。

单例 Bean 会被多个请求线程共享，因此 Controller、Service 等组件通常应该设计成**无状态对象**，不要把某个请求的用户信息、参数或临时计算结果保存在成员变量中，否则可能产生线程安全问题。

如果确实需要，也可以使用 `@Scope` 将 Bean 设置为 `prototype`、`request` 或 `session` 等作用域，但一般不建议随意修改 Controller 和 Service 的默认单例作用域。

---

## 6. 讲解一下 Bean 的生命周期

一个 Bean 从创建到销毁，主要经历以下阶段：

1. **实例化**：Spring 根据 BeanDefinition，通过构造方法或工厂方法创建 Bean 实例。
2. **属性填充**：完成依赖注入，把 Bean 所依赖的对象和值注入相应属性。
3. **执行 Aware 回调**：如果实现了相关接口，会调用对应方法，例如：
   - `BeanNameAware#setBeanName()`
   - `BeanFactoryAware#setBeanFactory()`
   - `ApplicationContextAware#setApplicationContext()`
4. **初始化前处理**：执行 `BeanPostProcessor#postProcessBeforeInitialization()`。
5. **执行初始化方法**：一般包括：
   - `@PostConstruct` 标注的方法；
   - `InitializingBean#afterPropertiesSet()`；
   - `init-method` 或 `@Bean(initMethod = "...")` 指定的方法。
6. **初始化后处理**：执行 `BeanPostProcessor#postProcessAfterInitialization()`。AOP 代理通常会在 Bean 后置处理阶段生成。
7. **Bean 就绪**：Bean 放入容器并对外提供使用。
8. **销毁**：容器关闭时，单例 Bean 会依次执行相关销毁回调，例如：
   - `@PreDestroy` 标注的方法；
   - `DisposableBean#destroy()`；
   - `destroy-method` 或 `@Bean(destroyMethod = "...")` 指定的方法。

简化记忆：

> **实例化 → 属性注入 → Aware 回调 → 初始化前置处理 → 初始化 → 初始化后置处理 → 使用 → 销毁**

补充：prototype Bean 在创建和初始化完成后就交给调用方，Spring 通常不会继续管理它的销毁过程。

---

## 7. 平时常用的两种代理是什么？

Spring AOP 常用的两种动态代理是 **JDK 动态代理** 和 **CGLIB 动态代理**。

### JDK 动态代理

- 基于接口实现。
- 要求目标类至少实现一个接口。
- 代理类会实现与目标类相同的接口。
- 核心类和接口是 `Proxy` 与 `InvocationHandler`。

### CGLIB 动态代理

- 基于继承实现。
- 通过生成目标类的子类完成方法增强。
- 不要求目标类实现接口。
- 由于依赖继承，不能代理 `final` 类，也不能增强无法被子类重写的 `final` 方法。

需要区分 Spring Framework 的一般选择规则和 Spring Boot 的默认配置：通常可以理解为有接口时可使用 JDK 动态代理，没有接口时使用 CGLIB；而 Spring Boot 默认配置通常会优先使用 CGLIB，可以通过配置进行调整。

两者本质上都是在运行期间创建代理对象，在调用目标方法前后加入事务、日志、权限等增强逻辑。

---

## 8. AOP 的通知类型都有哪些？

Spring AOP 主要有以下五种通知：

1. **前置通知 `@Before`**  
   在目标方法执行之前执行。

2. **后置通知 `@After`**  
   在目标方法结束后执行，无论方法正常返回还是抛出异常，通常都会执行，类似 `finally`。

3. **返回通知 `@AfterReturning`**  
   目标方法正常返回后执行，可以获取方法的返回值；如果方法抛出异常，则不会执行。

4. **异常通知 `@AfterThrowing`**  
   目标方法抛出异常后执行，可以获取抛出的异常。

5. **环绕通知 `@Around`**  
   在目标方法执行前后都可以加入逻辑，是功能最强的通知。通过调用 `ProceedingJoinPoint#proceed()` 决定是否以及何时执行目标方法，也可以修改参数或返回结果。

常见执行顺序可以理解为：

> `@Around` 前半部分 → `@Before` → 目标方法 → `@AfterReturning` 或 `@AfterThrowing` → `@After` → `@Around` 后半部分

实际项目中，声明式事务、接口日志、耗时统计、权限检查等功能都经常通过 AOP 通知实现。

## 9. RabbitMQ 比较重要的点有哪些？讲解一下 RabbitMQ 工作流程

> 参考：[[小林coding/消息队列面试题.md]]

### 核心概念

RabbitMQ 是基于 **AMQP（高级消息队列协议）** 实现的消息中间件。AMQP 只是一份“协议图纸”，规定了消息发出后不能直接进队列，中间必须经过**交换机（Exchange）**，再由路由键决定去哪个队列——这种灵活的路由能力正是 RabbitMQ 最大的王牌。

核心组件：

- **生产者（Producer）**：发送消息的一方。
- **交换机（Exchange）**：消息分发器。生产者不直接把消息发给队列，而是先发给交换机，交换机按规则把消息路由到队列。
- **队列（Queue）**：存储消息的缓冲区，消费者从这里取消息。
- **路由键（RoutingKey）与绑定（Binding）**：绑定是交换机和队列关联的纽带，路由键是消息携带的标识，交换机根据「路由键 + 绑定规则」决定投递到哪个队列。
- **消费者（Consumer）**：接收并处理消息的一方。

### 四种交换机类型

- **直连 Direct**：精准匹配，路由键完全一致才转发，适合一对一精准投递。
- **扇形 Fanout**：广播模式，不看路由键，绑定的队列全部收到，适合群发通知、多服务同步。
- **主题 Topic**：路由键模糊匹配，`*` 匹配一个单词、`#` 匹配多个或 0 个单词（如 `order.#`），最灵活，适合按业务主题分类。
- **头 Headers**：按消息头键值对匹配，性能差，几乎不用。

### 重要特性

- **持久化**：消息、队列、交换机都可持久化（`durable=true`），写入磁盘，重启不丢。
- **确认机制**：生产者确认（Publisher Confirm）保证消息到达交换机；消费者确认（ack）保证消息被处理后才从队列删除，未确认会重新入队。
- **镜像队列 / 集群**：队列内容复制到多个节点，防止单点故障，提高可用性。
- **死信队列 + 延迟队列**：处理失败/过期/队列满的消息可转入死信队列兜底；延迟队列用于订单超时取消等场景。

### 工作流程

> **生产者 → 交换机（Exchange）→（按 RoutingKey + Binding 路由）→ 队列（Queue）→ 消费者**

1. 生产者与 RabbitMQ 建立连接（Connection），并在连接上创建信道（Channel）。
2. 生产者把消息连同路由键发送给**交换机**（而不是直接发给队列）。
3. 交换机根据自己的类型、绑定关系和路由键，把消息路由到一个或多个**队列**。
4. 消息在队列中存储（若开启持久化则落盘）。
5. 消费者监听队列，取出消息进行处理。
6. 消费者处理完成后发送 **ack 确认**，RabbitMQ 才把消息从队列删除；若消费失败或超时未确认，消息重新入队或进入死信队列。

### 可靠性保障（三个环节）

- **生产者环节**：开启 Publisher Confirm，投递失败可重试。
- **服务器环节**：队列 + 消息持久化，集群/镜像队列防单点。
- **消费者环节**：手动 ack，处理成功再确认，配合死信队列兜底。

---

## 10. 对 Linux 操作有了解吗？说几个基础 Linux 命令

> 参考：[[小林coding/Linux命令面试题.md]]

按用途分类：

- **文件相关**：`mv`（移动/重命名）、`mkdir`（建目录）、`cd`（切目录）、`ls`（列目录）。
- **进程相关**：`ps`（查进程）、`top`（实时监控进程/CPU/内存）、`kill`（按 PID 杀进程）、`pkill`（按名字杀进程）。
- **权限相关**：`chmod`（改文件权限，如 `chmod 755 file`）、`chown`（改属主）、`useradd`（加用户）、`groupadd`（加组）。
- **网络相关**：`netstat`（查网络连接/端口占用）、`ip addr`（查网卡地址）、`lsof -i:80`（查端口被哪个进程占用）。
- **测试相关**：`ping`（测网络连通性）、`telnet` / `nc`（测端口连通性）。
- **文本处理**：`grep`（搜内容）、`sed`（替换，如 `sed -i 's/Hello/Hi/g' file`）、`awk`（按列处理）。

常见追问：`kill -9 PID` 强制杀进程；`top` 里按数字 `1` 查看 CPU 核心数；`free` 只看内存，`top` 还能看 CPU/进程/负载。

---

## 11. MySQL 事务隔离级别有哪些？

> 参考：[[小林coding/MySQL面试题.md]]

SQL 标准定义了四种隔离级别，隔离级别由低到高：

| 隔离级别 | 脏读 | 不可重复读 | 幻读 |
| --- | --- | --- | --- |
| 读未提交 Read Uncommitted | 可能 | 可能 | 可能 |
| 读已提交 Read Committed | 不会 | 可能 | 可能 |
| 可重复读 Repeatable Read（InnoDB 默认） | 不会 | 不会 | 可能\* |
| 串行化 Serializable | 不会 | 不会 | 不会 |

- **读未提交**：一个事务还没提交，它的变更就能被别的事务看到。
- **读已提交**：事务提交后，它的变更才能被别的事务看到。
- **可重复读**：事务执行过程中看到的数据，始终跟事务启动时一致。**InnoDB 默认级别**。
- **串行化**：对记录加读写锁，读写冲突时后一个事务必须等前一个完成。

三种并发问题：

- **脏读**：读到了另一个事务未提交的数据（对方回滚后就是脏数据）。
- **不可重复读**：同一事务内多次读同一数据，结果前后不一致（对方 update 并提交导致）。
- **幻读**：同一事务内多次按条件查询，记录**数量**前后不一致（对方 insert 导致）。

实现原理：

- 读未提交：直接读最新数据。
- 串行化：加读写锁避免并发。
- 读已提交 & 可重复读：靠 **MVCC 的 Read View** 实现，区别在生成 Read View 的时机——读已提交是**每条语句执行前**都重新生成，可重复读是**事务启动时**生成一个并用到底。

> \*补充：MySQL InnoDB 在可重复读级别下，通过 MVCC（快照读）+ 间隙锁 Next-Key Lock（当前读）很大程度上避免了幻读，这是它比 SQL 标准更强的地方。

---

## 12. 了解过 Java 的数据结构/集合吗？说几个

> 参考：[[小林coding/java/Java集合面试题.md]]

Java 集合框架分为 **Collection** 和 **Map** 两大体系。

### List（有序、可重复、有索引）

- **ArrayList**：底层动态数组，随机访问快（O(1)），尾部增删快，中间插入/删除要搬移元素。非线程安全。
- **LinkedList**：底层双向链表，可作双端队列。注意“增删比 ArrayList 快”是误区——任意位置增删仍需 O(n) 遍历定位，且缓存不友好，实测多数场景反而更慢，现在主流建议优先用 ArrayList。
- **Vector**：线程安全的 ArrayList，方法加 `synchronized`，已很少用。

### Set（不可重复、无序）

- **HashSet**：基于 HashMap 实现，用 key 保证唯一，不保证顺序，非线程安全。
- **LinkedHashSet**：基于 LinkedHashMap，用双向链表维护插入顺序。
- **TreeSet**：基于红黑树，元素有序。

### Map（键值对）

- **HashMap**：数组 + 链表 + 红黑树。JDK 1.8 后当链表长度 ≥ 8 **且**数组长度 ≥ 64 时才转红黑树，否则只扩容。非线程安全。
- **LinkedHashMap**：继承 HashMap，额外用双向链表维护插入顺序/访问顺序。
- **TreeMap**：红黑树，key 有序。
- **Hashtable**：线程安全但整个表加锁，性能差，已淘汰。
- **ConcurrentHashMap**：线程安全的 HashMap。JDK 1.7 分段锁（Segment），JDK 1.8 改为 CAS + `synchronized` 锁单个桶头节点，粒度更细。

线程安全集合：Hashtable、ConcurrentHashMap、CopyOnWriteArrayList、CopyOnWriteArraySet 等。

---

## 13. Java 最重要的三大特性是什么？讲解这三个特性

> 参考：[[小林coding/java/Java基础面试题.md]]

面向对象三大特性：**封装、继承、多态**。

### 封装（Encapsulation）

把对象的属性（数据）和行为（方法）结合在一起，对外隐藏内部实现细节，只通过对外暴露的接口（如 getter/setter）交互。目的是**增强安全性、简化使用、降低耦合**，让对象更独立。例如把字段设为 `private`，通过公有方法访问。

### 继承（Inheritance）

子类通过 `extends` 自动获得父类的字段和方法，是**代码复用**的重要手段，能建立类之间的层次关系。Java 是单继承（一个类只能继承一个父类），但可以实现多个接口。

### 多态（Polymorphism）

同一个接口/父类引用，指向不同子类实例时表现出不同行为——“同一消息，不同响应”。分两种：

- **编译时多态（重载 Overload）**：同名方法不同参数，编译期确定。
- **运行时多态（重写 Override）**：子类重写父类方法，运行时 JVM 根据对象**实际类型**决定调用哪个版本，这是多态的主要体现。

多态的前提：**继承/实现接口 + 方法重写 + 父类引用指向子类对象**。它提高了代码的扩展性和复用性，是很多设计模式（策略模式）、设计原则（依赖倒置、里氏替换、基于接口编程）的基础，也能用来消除冗长的 if-else。

---

## 14. 了解 Java 版本以及新版本特性吗？

> 参考：[[小林coding/java/Java基础面试题.md]]（小林 coding 主要覆盖 Java 8 新特性，其余版本为补充）

### 版本节奏

Java 从 9 开始改为**每 6 个月发布一个新版本**，其中每隔几个版本会有一个 **LTS（长期支持）版本**。目前主流 LTS：**Java 8、11、17、21、25**。

### 各版本关键特性

- **Java 8（里程碑，最常用）**：Lambda 表达式、Stream API、函数式接口（`@FunctionalInterface`）、方法引用（`System.out::println`）、Optional（防空指针）、接口默认方法/静态方法、新的日期时间 API（`LocalDate` 等）、CompletableFuture。
- **Java 9**：模块化系统（Jigsaw）、接口私有方法、JShell、集合工厂方法 `List.of()`。（`new Integer()`、`Class.newInstance()` 等自 9 起被 `@Deprecated`）
- **Java 11（LTS）**：局部变量 `var`（10 引入、11 支持用于 Lambda 参数）、标准化的新 HTTP Client、`String` 增强（`strip()`、`isBlank()`、`lines()`）、可直接 `java` 运行单文件源码。
- **Java 17（LTS）**：密封类 `sealed`、`instanceof` 模式匹配、Record 记录类（简化不可变数据类）、文本块 `"""..."""`。
- **Java 21（LTS）**：虚拟线程（Virtual Threads，轻量级线程，大幅提升高并发吞吐）、`switch` 模式匹配转正、Record 模式、顺序集合 SequencedCollection。
- **Java 25（LTS，2025-09 发布）**：距 21 两年后的新 LTS，Oracle 提供至少 8 年支持。带来 AOT 事前缓存加速启动、JIT 即时生成本地代码等性能改进，并进一步降低初学者门槛（简化 `main` 方法、隐式导入等）。

一句话记忆：**8 是函数式编程分水岭，17 带来 Record/密封类/模式匹配，21 带来虚拟线程，25 是最新 LTS。**



