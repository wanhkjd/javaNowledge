我已读完简历，下面以 Java 后端实习面试官的身份开始模拟面试。规则说明：我每轮问 1～2 个问题，你作答后我会追问、点评，并继续下一轮。回答时可以像真实面试一样口头表达，不必写得很正式。答不上来可以直接说"不清楚"，我会给出提示或参考答案。

---

**面试官**：你好，万同学，请坐。我是负责 Java 后端方向的面试官，今天大概聊 40 分钟左右，主要围绕你的项目和基础知识。

先来第一个问题：

**Q1. 请先用 1～2 分钟做个自我介绍，重点讲讲你的技术方向和你觉得最拿得出手的一个项目。**

**Q2. 我看你简历上两个项目，一个是小说平台，一个是商城微服务。如果只能选一个深入聊，你希望聊哪个？为什么是这个？**

我是 2027 届学生，主要学习和实践 Java 后端，重点梳理了 novel 小说平台和 hmall 商城两个项目。   
novel 偏读密集型业务，涉及章节正文分表、缓存、全文检索和 AI 业务工具；
hmall 偏交易业务，涉及网关鉴权、服务间调用、下单事务和支付消息。  
我比较关注的不只是组件能否接入，而是请求如何执行、并发下是否正确、失败后如何恢复。

---
**Q3. 先讲清楚流程。** 用户点击"提交订单"之后，请求从网关进来，一直到订单落库、库存扣减完成，中间经过了哪些服务、哪些步骤？请按顺序说，说到每一步时顺便说一下这一步失败了会怎样。

**Q4. 追问一个具体点。** 你简历上写"库存扣减采用数据库原子更新语句"。请把这条 SQL 大概写出来，并回答：既然已经用了原子更新，为什么还需要 Seata 的全局事务？这两者分别解决什么问题？

请求路径是：**网关 → trade-service → item-service / cart-service → RabbitMQ**。

1. **网关层**。请求先经过 Gateway 的全局过滤器：校验 JWT，解析出 userId 后放进请求头透传给下游。失败场景：token 无效直接 401，请求不会进入业务服务。
2. **trade-service 入口**。拦截器把请求头里的 userId 放进 ThreadLocal（UserContext），后续下单用它填订单的 userId。
3. **合并商品数量**。把前端传的明细按 itemId 聚合，同一商品出现多次数量累加。这一步是为了防 `Collectors.toMap` 遇到重复 key 抛异常。
4. **远程查商品**。通过 OpenFeign 调 item-service 查询商品，请求拦截器把 userId 继续透传。失败场景：item-service 挂了，FallbackFactory 对"查询"降级返回空列表，我在下单侧判断"查到的数量少于要买的数量"就抛"商品不存在"，拒绝下单，而不是让空指针炸出来。
5. **服务端算价、订单落库**。金额以服务端查到的价格乘数量，不信任前端传的金额。然后写 order 表和 order_detail 表。
6. **清理购物车**。远程调 cart-service 删除对应条目。
7. **扣减库存**。远程调 item-service，用原子 SQL 扣减。失败场景分两种：库存不足是业务异常，ErrorDecoder 把下游的 code/msg 还原后原样抛出；网络超时是系统异常，FallbackFactory 对"写入"不降级，直接抛。两种都会让整个方法抛异常。
8. **全局事务回滚**。整个 createOrder 加了 `@GlobalTransactional`，Seata AT 模式下，第 5～7 步任一步抛异常，订单、明细、购物车删除、已扣的库存都会回滚。
9. **发延迟消息**。最后往 RabbitMQ 延迟交换机发订单 ID，15 分钟后消费端检查支付状态，未支付则走关单流程：先关支付单，再条件更新订单状态为已关闭，最后回补库存。

**面试官视角提醒你两个坑，很可能被追问：**

- **第 9 步发消息在事务内**。如果消息发出去了但事务随后回滚（比如你把发消息放到扣库存前面），消费端 15 分钟后会去关一个不存在的订单。你现在的代码把发消息放在最后一步，且 Seata 全局提交是在方法返回之后，所以严格来说还是有一个很小的窗口：消息已发、全局提交失败。真实回答里要主动说"这里用状态条件更新兜底，关单时查不到未付款订单就直接忽略"。
- **第 6 步清购物车放在扣库存之前**。合理的顺序是先扣库存再清购物车，因为库存失败概率高得多，早失败少回滚。这是可以主动说出来的优化点。


---

## Q3 参考答案：下单主链路

**（面试时口头版本，约 2 分钟）**

请求路径是：**网关 → trade-service → item-service / cart-service → RabbitMQ**。

1. **网关层**。请求先经过 Gateway 的全局过滤器：校验 JWT，解析出 userId 后放进请求头透传给下游。失败场景：token 无效直接 401，请求不会进入业务服务。
2. **trade-service 入口**。拦截器把请求头里的 userId 放进 ThreadLocal（UserContext），后续下单用它填订单的 userId。
3. **合并商品数量**。把前端传的明细按 itemId 聚合，同一商品出现多次数量累加。这一步是为了防 `Collectors.toMap` 遇到重复 key 抛异常。
4. **远程查商品**。通过 OpenFeign 调 item-service 查询商品，请求拦截器把 userId 继续透传。失败场景：item-service 挂了，FallbackFactory 对"查询"降级返回空列表，我在下单侧判断"查到的数量少于要买的数量"就抛"商品不存在"，拒绝下单，而不是让空指针炸出来。
5. **服务端算价、订单落库**。金额以服务端查到的价格乘数量，不信任前端传的金额。然后写 order 表和 order_detail 表。
6. **清理购物车**。远程调 cart-service 删除对应条目。
7. **扣减库存**。远程调 item-service，用原子 SQL 扣减。失败场景分两种：库存不足是业务异常，ErrorDecoder 把下游的 code/msg 还原后原样抛出；网络超时是系统异常，FallbackFactory 对"写入"不降级，直接抛。两种都会让整个方法抛异常。
8. **全局事务回滚**。整个 createOrder 加了 `@GlobalTransactional`，Seata AT 模式下，第 5～7 步任一步抛异常，订单、明细、购物车删除、已扣的库存都会回滚。
9. **发延迟消息**。最后往 RabbitMQ 延迟交换机发订单 ID，15 分钟后消费端检查支付状态，未支付则走关单流程：先关支付单，再条件更新订单状态为已关闭，最后回补库存。

**面试官视角提醒你两个坑，很可能被追问：**

- **第 9 步发消息在事务内**。如果消息发出去了但事务随后回滚（比如你把发消息放到扣库存前面），消费端 15 分钟后会去关一个不存在的订单。你现在的代码把发消息放在最后一步，且 Seata 全局提交是在方法返回之后，所以严格来说还是有一个很小的窗口：消息已发、全局提交失败。真实回答里要主动说"这里用状态条件更新兜底，关单时查不到未付款订单就直接忽略"。
- **第 6 步清购物车放在扣库存之前**。合理的顺序是先扣库存再清购物车，因为库存失败概率高得多，早失败少回滚。这是可以主动说出来的优化点。

---

## Q4 参考答案：原子更新 vs Seata

**SQL 是这条：**

```sql
UPDATE item
SET stock = stock - #{num}
WHERE id = #{itemId} AND status = 1 AND stock >= #{num}
```

Java 侧检查影响行数，不等于 1 就抛异常。影响行数为 0 有三种含义：商品不存在、已下架、库存不足，统一按失败处理。

**两者解决的是不同层面的问题：**

||原子更新语句|Seata 全局事务|
|---|---|---|
|解决什么|**单行的并发正确性**。避免"先查后减"的超卖，用行锁 + 条件让扣减在数据库层串行化|**跨服务的原子性**。订单在 trade 库、库存在 item 库、购物车在 cart 库，本地事务管不到别的库|
|不用它会怎样|两个请求同时读到 stock=1，都减成 0，超卖|库存扣成功了，但订单落库后面的步骤失败，出现"库存扣了、订单没了"或反过来|
|作用范围|一条 SQL、一个库|一次 RPC 链路上的多个数据库|

一句话总结给面试官：**原子更新保证"单次扣减不会扣错"，Seata 保证"扣减和下单要么一起成功要么一起失败"。前者是并发问题，后者是一致性问题，谁也替代不了谁。**

**必然会被追问的一层：Seata AT 模式怎么回滚库存？**

AT 模式在执行扣减 SQL 前后各查一次这行数据，生成 before/after 镜像存进 undo_log。回滚时用 after 镜像和当前数据比对，一致就按 before 镜像反向 UPDATE。这里有个隐患要主动说：**如果回滚前有另一个订单又扣了这行库存，after 镜像和当前值对不上，AT 会回滚失败，需要人工介入**。Seata 靠全局锁避免这种情况，但全局锁只对同样走 Seata 的写生效，绕过 Seata 直接改库存就管不住。

---

## 面试官点评

这两题在你简历里都有对应的实现，且代码里的注释写得比较到位，说明你确实理解。真实面试中的风险在于：**你需要把"流程"和"失败处理"交织着讲，而不是先讲一遍流程再等面试官问失败**。上面 Q3 每一步后面跟一句"失败了会怎样"，就是这种讲法。

---

**继续下一轮。既然聊到了 Seata 和消息，我顺着往下问：**

**Q5.** 你说延迟消息 15 分钟后触发关单。假设关单流程执行到"回补库存"时 item-service 超时了，这条消息会怎么处理？重试后整个 cancelOrder 重新执行，"关闭支付单"这一步会被调用两次，会不会有问题？

**Q6.** 换个方向考基础。你简历写"熟悉 ThreadLocal 用户上下文传递"。UserContext 里的 ThreadLocal 如果拦截器在请求结束时忘了 remove，在 Tomcat 线程池下会出现什么问题？另外，OpenFeign 调下游时，为什么下游拿不到上游 ThreadLocal 里的 userId，你是怎么解决的？

我再看一下关单消费者、支付单关闭逻辑、UserContext 和 Feign 拦截器的实际代码，再给你参考答案。

我已核对完关单监听器、支付单关闭逻辑、Feign 拦截器和 UserContext 的代码，下面是对照你实际实现的参考答案。

---

## Q5 参考答案：关单重试与"关闭支付单"重复调用

**第一层：消息会怎么处理**

回补库存时 item-service 超时，FallbackFactory 对 restoreStock 这类写操作不做降级，而是直接抛异常。异常从 cancelOrder 冒出来，触发两件事：

1. **Seata 回滚**。cancelOrder 加了 `@GlobalTransactional`，前面那步"订单状态改为已关闭"的本地更新会被回滚，订单退回未付款状态。
2. **消费者重试**。trade-service 配置了 Spring AMQP 的本地重试：初始间隔 1 秒，倍数 2，最多 3 次。3 次耗尽后交给 MessageRecoverer 投到错误队列，等人工处理或定时任务补偿，不会无限重试拖垮消费者。

代码里还有一个细节值得主动讲：Sentinel 会把 Feign fallback 抛出的异常包装成 AssertionError，而 Spring AMQP 消费线程遇到 Error 默认会退出整个 JVM。所以监听器里 catch 了 Error 转成 RuntimeException，否则一次下游超时能把整个交易服务打挂。这一句说出来，面试官会知道你真的踩过坑。

**第二层：closePayOrderByBizOrderNo 被调用两次有没有问题**

没有问题，这个方法本身设计成幂等的，分四种情况：

|支付单状态|行为|返回|
|---|---|---|
|不存在（用户没申请过支付）|无事可做|true|
|已支付成功|不能关，告诉调用方改标记已支付|false|
|已关闭|重复请求，直接返回|true|
|待支付|更新为已关闭|true|

第一次执行已经把支付单改成"已关闭"，重试时落到第三种情况，直接返回 true，不产生副作用。

**第三层：为什么支付单不放进 Seata 全局事务，而是靠幂等**

这是面试官最想听的。支付单关闭虽然也是写操作，但它没有走 Seata 分支事务，原因是**关闭支付单必须先于关单持久化，且不能被回滚**。如果关单失败回滚了"支付单已关闭"，用户就能在订单退回未付款期间付款成功，而下一次重试又把订单关了，出现"付了钱但订单关闭"的最坏情况。所以设计成：支付单关闭是不可逆的第一步，靠幂等应对重试；订单和库存是可回滚的第二、三步，靠 Seata 应对部分失败。

还有一个并发保护：关闭支付单时用 `SELECT ... FOR UPDATE` 锁住支付单行，而余额支付也用同一把行锁。付款和关单同时到达时会串行化，只能有一个成功，不会出现两边各自读到"待支付"然后各自成功。

---

## Q6 参考答案：ThreadLocal 与身份透传

**第一部分：忘记 remove 会怎样**

Tomcat 用线程池处理请求，线程是复用的。ThreadLocal 的值挂在线程对象的 ThreadLocalMap 上，线程不销毁，值就不消失。忘记 remove 会产生两个问题：

1. **数据串号**。请求 A 是用户 1001 处理完没清理，线程被请求 B 复用。如果 B 是白名单接口（没带 user-info 头），拦截器不会 set，B 就会读到 1001，把 B 的操作算到用户 1001 头上。这是安全问题，比内存问题更严重。
2. **内存泄漏**。ThreadLocalMap 的 key 是弱引用，value 是强引用。ThreadLocal 对象被回收后 key 变 null，但 value 还被 Entry 持有，只有下次 set/get/remove 触发探测式清理才可能释放。线程池里的线程长期存活，value 就一直泄漏。项目里 UserContext 是静态字段，ThreadLocal 本身不会被回收，所以主要是第一种问题。

项目里的处理：拦截器在 afterCompletion 里调 remove。要说清楚为什么是 afterCompletion 而不是 postHandle：postHandle 在 Controller 抛异常时不会执行，afterCompletion 无论成功失败都会执行。

**第二部分：Feign 调用为什么下游拿不到 userId**

ThreadLocal 是线程私有、进程内的。Feign 调用是一次新的 HTTP 请求，下游服务在另一个 JVM 里，由另一个线程池处理，根本不共享内存。上游的 ThreadLocal 里有什么，下游一无所知。

解决方式是**把身份信息显式放进 HTTP 头，再在下游还原**，链路是三段：

1. **网关**解析 JWT，把 userId 写进 user-info 请求头转发给第一个业务服务。
2. **业务服务**的 MVC 拦截器读取 user-info 头，set 进 ThreadLocal。
3. **Feign 的 RequestInterceptor**在发起远程调用前，从 ThreadLocal 取 userId 再写进 user-info 头。下游服务的拦截器再重复第 2 步。

**必然被追问的一层：这个方案的漏洞**

- **异步线程拿不到**。如果下单流程里用 `@Async` 或自己开线程池去调 Feign，子线程的 ThreadLocal 是空的，请求头里没有 userId。解决方案是 InheritableThreadLocal 或阿里的 TransmittableThreadLocal，前者只在创建线程时拷贝一次，线程池复用时失效，所以线程池场景要用后者。
- **内部信任问题**。下游只认 user-info 头，谁都可以直接调下游接口伪造这个头。项目里靠网关是唯一入口，业务服务不暴露公网端口来兜底。更严谨的做法是内部调用带签名或走 mTLS。
- **MQ 消费线程也没有**。消费者线程处理关单消息时 UserContext 是空的，这也是为什么 markOrderPaySuccess 和 cancelOrder 不依赖当前用户，全靠订单 ID 查数据。

---

## 面试官点评

- Q5 你的代码实现比多数校招生完整，关键是要**把"为什么支付单不进全局事务"这个设计决策讲出来**，而不只是说"它是幂等的"。设计决策才是区分度。
- Q6 的 ThreadLocal 内存泄漏是高频题，但更多面试官在意的是**数据串号**这个业务后果，先说串号再说泄漏，会显得你是从业务角度思考。

---

**下一轮换到 novel 项目和数据库基础：**

**Q7.** 你在 novel 里把章节正文按 `chapter_id % 10` 分了 10 张表。为什么按 chapter_id 而不是按 book_id 分？如果产品要做"某本书的全部章节正文导出"，你现在这个分片键会带来什么问题？

**Q8.** 你简历写"了解索引、事务、锁与 SQL 优化"。有一张订单表，查询是 `WHERE user_id = ? AND status = ? ORDER BY create_time DESC LIMIT 10`。你会建什么索引？三个列的顺序怎么定，为什么？

我先看一下 novel 项目里分表配置和相关表结构，再给参考答案。

我已核对 novel 的分表配置和表结构，关键事实是：`book_content` 表只有 id、chapter_id、content 三个业务列，**根本没有 book_id 字段**，分片键只能在 chapter_id 上选。下面是参考答案。

---

## Q7 参考答案：为什么按 chapter_id 分片

**第一层：直接回答"为什么"**

三个理由，按重要性排：

1. **查询模式决定分片键**。正文表 99% 的读请求是"阅读某一章"，SQL 是 `WHERE chapter_id = ?`。分片键和查询条件一致，ShardingSphere 能直接算出落在哪张物理表，单表查询、不广播。用 book_id 分片的话，阅读接口只带 chapter_id，路由不出来，只能 10 张表全扫再合并。
2. **表结构里没有 book_id**。book_content 只存章节 ID 和正文，书和章节的关系在 book_chapter 表里。按 book_id 分片要先加冗余列，还要保证每次写入都带上，改动面大。
3. **数据更均匀**。章节 ID 是雪花 ID 连续生成，取模后 10 张表各分十分之一。按 book_id 取模会出现热点：一本万章的长篇和一本十章的短篇被分到同一张表，表大小差距悬殊，而且热门书的全部读流量集中打一张表。

**第二层：导出全书正文会遇到什么问题**

导出的 SQL 是先从 book_chapter 查出这本书的所有 chapter_id，再 `WHERE chapter_id IN (...)`。问题有三个：

- **跨分片查询**。一本书的章节 ID 连续，取模 10 后均匀散落在全部 10 张表，一次导出必然打所有分片。ShardingSphere 会拆成 10 条 SQL 并行执行再内存归并，性能是单表的数倍开销，且归并结果占用应用内存。
- **IN 列表膨胀**。上千章的书 IN 里塞上千个值，SQL 本身很长，MySQL 解析和优化器成本上升，有的中间件对 IN 的元素数量还有上限。
- **排序在应用层做**。导出要按章节顺序，而 book_content 没有序号列，顺序信息在 book_chapter 里。跨分片的结果拿回来后必须在内存里按章节序重排。

**第三层：怎么解决，这是面试官真正想听的**

导出是低频、离线、可容忍延迟的操作，不应该为它改分片键去牺牲高频的阅读接口。合理做法是：

- **分批走单分片**。按章节顺序把 chapter_id 列表切成小批，每批查一次，每次 IN 的值少，内存可控。虽然还是跨分片，但避免了一次性归并全书。
- **异步任务加文件产出**。导出丢给 XXL-Job 之类的异步任务，逐章读、边读边写文件，完成后给个下载链接，不占用同步请求。
- **如果导出真成了高频需求**，改用 book_id 作分片键，让同一本书的正文落在同一张表，阅读接口也改成 `WHERE book_id = ? AND chapter_id = ?` 带上两个字段。这是用复合分片键同时满足两种查询的标准做法，代价是加冗余列和数据迁移。

一句话收尾：**分片键选高频查询的条件列，低频场景用异步和分批绕过，而不是反过来。**

---

## Q8 参考答案：订单表索引设计

**结论先说**：建联合索引 `(user_id, status, create_time)`，三列按这个顺序。

**为什么是这个顺序**

- **等值条件放前面**。user_id 和 status 都是 `=` 条件，放在索引前两列，B+ 树能直接定位到 `user_id=? AND status=?` 的那一段连续区间。
- **范围和排序列放最后**。create_time 用于 ORDER BY，放在等值列之后，这个区间内的数据在索引里天然按 create_time 有序，MySQL 直接顺着索引取 10 条，执行计划里不会出现 Using filesort。ORDER BY 是 DESC，MySQL 8 可以反向扫描索引，或者直接建降序索引。
- **user_id 和 status 谁在前**。两者都是等值条件，理论上顺序都能用到。实际上把 user_id 放前面，因为它区分度高，且大量其他查询只按 user_id 过滤，比如"我的全部订单"，最左前缀原则让这条索引能被复用。status 只有几个值，单独放最前面基本没有筛选力。

**如果顺序放错会怎样**

|索引|后果|
|---|---|
|(create_time, user_id, status)|排序列在前，等值条件用不上索引筛选，得扫大量行|
|(user_id, create_time, status)|只能用到 user_id，create_time 后面的 status 断了最左前缀，status 要回表过滤，且排序倒是能利用|
|(status, user_id, create_time)|能用，但 status 区分度低，且"只查 user_id"的查询用不上这条索引|

**加分项：主动说三点**

1. **status 有时会写 IN**，比如查"待付款或待发货"。此时 status 变成范围条件，后面的 create_time 就不能用于排序了，会退化成 filesort。MySQL 8 对 IN 做了 range 优化，少量值还能走索引，值多了就不行。这种情况可以把 status 从索引里去掉，改成 `(user_id, create_time)` 然后回表过滤 status。
2. **深分页问题**。LIMIT 10 没问题，LIMIT 100000, 10 会扫 100010 条再丢掉前面的。解决办法是把上一页最后一条的 create_time 带过来，改成 `WHERE ... AND create_time < ? LIMIT 10` 的游标翻页。
3. **覆盖索引**。如果查询只需要订单 ID 和金额这几个字段，可以把它们追加到索引里做覆盖索引，避免回表。但订单表列多，通常不这么做。

---

## 面试官点评

- Q7 你要**先说清楚"表里没有 book_id"这个事实**，说明你真看过表结构，而不是背中间件配置。然后再讲查询模式，导出问题从"低频离线"角度切入给方案，不要一上来就说改分片键。
- Q8 是基础题，区分度在于能不能主动讲 status 变 IN 之后索引失效、以及深分页。说到 Using filesort 和最左前缀这两个词，面试官会认可你实操过 EXPLAIN。

---

**下一轮，聊缓存和消息一致性，再补一道基础：**

**Q9.** novel 里章节正文缓存在 Redis，作者修改章节后你"删除对应缓存"。假设修改事务提交前有一个读请求刚好把旧正文重新写回了缓存，会发生什么？你的项目里有没有应对，如果没有，你会怎么做？

**Q10.** HashMap 在 JDK 8 里 put 一个元素的完整过程是什么？什么情况下链表会转红黑树，为什么阈值是 8？

上一次读取缓存配置文件时工具出错了，我用 shell 重新读一下，然后给出参考答案。

我已核对 novel 的缓存读写代码，关键事实有三个：读路径用 `@Cacheable` 走 Redis 缓存管理器，写路径在事务方法里调 `@CacheEvict`，而缓存配置里开了 `setTransactionAware(true)`。这个配置决定了 Q9 的答案走向。

---

## Q9 参考答案：缓存删除与旧值写回

**先回答"会发生什么"，分两种时序说。**

**时序一，也就是面试官描述的场景：删除动作在事务提交前执行。** 写线程更新了正文但还没提交，删缓存。读线程恰好 miss，去查库，由于事务未提交，隔离级别下读到的是旧正文，然后把旧正文写回 Redis。写线程提交。结果是 Redis 里存着旧正文，而正文缓存的 TTL 是 12 小时，读者要看 12 小时旧章节。这是"事务内删缓存"的经典错误。

**项目里对这个时序有应对，就是 Redis 缓存管理器的 transactionAware 开关。** 开启后 Spring 会用 TransactionAwareCacheDecorator 包装每个缓存，事务内发起的 evict 和 put 不会立刻执行，而是注册到事务同步器的 afterCommit 里，提交之后才真正删 Redis。所以实际时序是：更新库，提交，删缓存。读线程在提交前写回的旧值，会被提交后的那次删除清掉。这和项目里 MQ 消息用 afterCommit 发送是同一个思路，所有对外副作用都放到提交之后。

补充两个细节让回答更完整。`@CacheEvict` 默认 beforeInvocation 为 false，方法抛异常不删缓存。事务回滚时 afterCommit 也不会触发。回滚场景下这两个行为都是对的，库没变，缓存自然不该动。

**时序二，项目没覆盖的窗口。** 读线程先 miss 并查库拿到旧正文，但因为 GC 停顿或网络抖动，还没来得及写 Redis。写线程完成更新、提交、删缓存。然后读线程才把旧正文写进 Redis。这个窗口要求一次读库的耗时超过一次完整的写事务，概率很低，但确实存在。Cache Aside 模式在理论上就有这个窗口，任何删缓存方案都躲不开，只能缩小影响。

**怎么做，按代价从低到高。**

1. **延迟双删。** 提交后删一次，再延迟几百毫秒到一秒异步删第二次，把慢读线程写回的旧值清掉。项目里 RabbitMQ 已经在用，发一条延迟消息就能实现。这是我会优先加的。
2. **缩短兜底 TTL。** 正文 12 小时偏长，可以按业务容忍度压短。这只是把不一致时间从 12 小时压到 TTL 长度，治标。
3. **写回带版本。** 缓存值里带更新时间，写回时用 Lua 脚本比较版本，旧版本不覆盖新版本。改动大，正文场景不值得。
4. **读路径加锁。** 用项目里现成的 Redisson 锁切面，miss 时按章节 ID 加锁再回源，能顺便解决热点章节的击穿。但要说清楚：它只解决击穿，不解决慢读写回，除非写路径也拿同一把锁，那样会拖累读吞吐。
5. **订阅 binlog。** Canal 监听正文表变更后删缓存，时机一定在提交后，且与业务代码解耦。对这个项目太重。

结论一句话：正文修改是低频操作，读者短时间看到旧内容可以接受，所以提交后删除加延迟双删加 TTL 兜底就够了。

**加分项，主动说本地缓存的多实例问题。** 章节信息和小说信息走的是 Caffeine 本地缓存，`@CacheEvict` 只清当前实例。部署多实例时，其他实例要等 TTL 过期才看到新数据。

|缓存|类型|TTL|
|---|---|---|
|章节正文|Redis|12 小时|
|章节信息|Caffeine 本地|6 小时|
|小说信息|Caffeine 本地|18 小时|

项目里的 MQ 消息目前只用来同步 ES，没有广播本地缓存失效。解决办法是让每个实例都消费小说变更消息并清本地缓存，用广播模式的交换机或 Redis 的发布订阅。能主动说出这一点，说明你知道本地缓存的边界在哪。

---

## Q10 参考答案：HashMap 的 put 与树化

**put 的核心是 putVal，按顺序八步。**

1. **算 hash。** 取 key 的 hashCode，高 16 位异或低 16 位。桶下标只用低几位，不扰动的话高位不同、低位相同的 key 会全撞进同一个桶。null 的 hash 是 0，固定落在第 0 个桶。
2. **表为空就 resize 初始化。** HashMap 是懒初始化，构造时不建数组，第一次 put 才建。默认容量 16，阈值 12。
3. **算下标。** 用容量减一与 hash 做按位与。因为容量是 2 的幂，这等价于取模但快得多。桶为空就直接放新节点。
4. **桶不为空，分三种情况。** 首节点 hash 相等且 key 相等，记为已存在。首节点是 TreeNode，走红黑树插入。否则遍历链表，边走边比对 key，走到尾没找到就尾插。JDK 7 是头插，并发扩容会成环导致 get 死循环，JDK 8 改尾插修掉了这个死循环，但 HashMap 依然不是线程安全的。
5. **尾插后链表长度达到 8，调用 treeifyBin。**
6. **key 已存在时替换 value 并返回旧值。** 这条路径不改 modCount 也不加 size。
7. **modCount 加一，size 加一，超过阈值就 resize 翻倍。**
8. **resize 不重新算 hash。** JDK 8 用 hash 与旧容量做按位与，结果为 0 留在原位，否则挪到原下标加旧容量的位置，链表相对顺序不变。

**链表转树有两个条件，缺一不可。**

|条件|要求|不满足时|
|---|---|---|
|单个桶的链表长度|达到 8|继续用链表|
|数组容量|至少 64|不转树，改为扩容|

第二条经常被漏掉。容量小的时候链表长，更可能是数组太小造成的，扩容把节点摊开比建树更划算。

**为什么阈值是 8，三个理由。**

1. **泊松分布。** 负载因子 0.75 时，理想哈希下每个桶的节点数服从参数 0.5 的泊松分布，长度到 8 的概率约千万分之六。正常情况下几乎不会树化，它是对付坏 hashCode 和哈希碰撞攻击的兜底，不是常态路径。
2. **空间代价。** TreeNode 比普通节点多了父、左、右、前驱四个引用和颜色位，体积约两倍。链表很短时转树纯亏空间。
3. **时间平衡。** 长度 8 的链表平均查 4 次，红黑树查 3 次，从这个长度起树才有收益。

还有一个数值要主动说：退化阈值是 6，不是 8。中间留出 2 的缓冲，防止元素在 8 附近增删时树和链表来回转换。退化发生在 resize 拆分桶的时候，拆出来的树节点数不超过 6 就退回链表。

**加分项，主动带一句。**

- **容量为什么是 2 的幂。** 位运算代替取模，扩容拆链表只看一位。传入的初始容量会被向上取到 2 的幂。
- **负载因子为什么是 0.75。** 空间和时间的折中，太小浪费空间频繁扩容，太大碰撞多。0.75 乘 2 的幂总是整数，阈值好算。
- **key 不可比较时红黑树怎么排序。** 先比 hash，再看是否实现 Comparable，都不行就用 identityHashCode 做最终裁决。
- **线程安全。** 多线程 put 会丢数据、size 不准，要用 ConcurrentHashMap。

## 面试官点评

- **Q9 的区分点是知道 transactionAware 在起作用。** 很多人用了 Spring Cache 但不知道 evict 被延迟到了提交后，答不出"项目里有应对"。同时要坦白说慢读写回的窗口没覆盖，比硬说"没问题"更可信。
- **Q10 的区分点是容量 64 这个第二条件和退化阈值 6。** 能说出泊松分布，说明你看过源码注释而不是背博客。

---

**下一轮，继续一道项目题加一道基础题：**

**Q11.** novel 里章节改动后发 MQ 消息，消费端按小说 ID 更新 ES 文档。作者在几秒内连续两次修改同一本书，两条消息可能被两个消费者线程并发处理，或者乱序到达。ES 里最后会不会是旧数据？如果消息重复投递，会不会重复更新？你怎么保证最终一致？

**Q12.** 简历写了解 JVM 内存结构和垃圾回收。一个对象从 new 出来到被回收，经历了哪些内存区域？什么情况下对象会直接进入老年代？线上遇到频繁 Full GC，你会怎么排查？

我先看一下 novel 里 ES 同步消费者的实际实现，再给参考答案。

我已核对 novel 的 MQ 链路：交换机是 fanout，消息体只有小说 ID，消费者收到后重新查库、用 `esClient.index` 整篇覆盖写入 ES。消费者端没有配并发数、重试和死信，生产者端只配了模板重试。这几个事实决定了 Q11 的答案。

---

## Q11 参考答案：ES 同步的并发、乱序与重复

**先说结论：连续修改在当前配置下不会留下旧数据，重复投递也不会出错。但多实例部署后有一个乱序窗口，我知道它在哪，也知道怎么补。**

**为什么现在是安全的，核心是一个设计选择：消息只带 ID，不带数据。**

消费者拿到小说 ID 后重新查库，再把整条小说信息覆盖写进 ES。这意味着写进 ES 的内容由消费那一刻的数据库状态决定，和消息是什么时候发的没关系。两条消息不管谁先谁后，只要最后一次写 ES 的那次读库发生在两次修改都提交之后，ES 就是最新的。消息是在事务 afterCommit 之后才发的，所以第二条消息存在时，两次修改一定都已经提交。

再加上消费者端没配并发数，Spring AMQP 默认每个监听器一个消费线程，队列里的消息串行处理。第一条处理完整个"读库加写 ES"之后才轮到第二条，写 ES 的顺序等于消息顺序，最后一次写读到的就是最终状态。

**重复投递**同样靠这个设计兜住。ES 的 index 操作按文档 ID 整篇覆盖，本身幂等。重复消息只是再查一次库、再写一遍相同的最新数据，不需要去重表。

**乱序窗口在哪。** 部署两个实例后，同一个队列有两个消费者，RabbitMQ 轮询分发。消费者 A 拿到第一条，在第二次修改提交之前读库，拿到旧数据。消费者 B 拿到第二条，读到新数据并写入 ES。然后 A 因为 GC 停顿或网络慢，此时才把旧数据写进 ES。结果 ES 是旧的，且不会自愈，要等下一次修改。

**保证最终一致，我会做三件事，按优先级排。**

1. **给 ES 写入带外部版本号。** 小说表有 update_time，把它转成毫秒时间戳作为 external 版本传给 index 请求。ES 只接受版本号大于等于当前值的写入，旧数据的写入会返回 409 冲突，消费者把这个冲突当成功吞掉。这是解决乱序最标准的办法，成本只有两行代码。用 external_gte 而不是 external，这样重复消息带相同版本也能写成功，不会被误判为冲突。
2. **加消费者端重试和死信队列。** 现在监听器用 SneakyThrows 把异常直接抛出，Spring AMQP 默认拒收并重新入队，一条坏消息会无限重投。比如小说已被删除，查库返回 null，取 ID 时空指针，这条消息会一直循环。要配置有上限的重试，耗尽后进死信队列。同时 null 的情况应该改成删除 ES 文档，而不是报错。
3. **补一个对账任务。** 消息在提交之后发，如果这时 MQ 挂了，模板重试三次后抛异常，但事务已经提交，数据库改了而 ES 没同步。项目里已有 XXL-Job，加一个定时任务，按 update_time 扫最近几分钟改过的小说重新索引。ES 是派生数据，允许短暂落后，用对账兜底比引入本地消息表更轻。

**一个可以主动指出的细节。** afterCommit 里抛出的异常会一直传到调用方，也就是说 MQ 不可用时接口会返回 500，但数据库其实已经改成功了。作者看到报错会重新提交一次，产生第二次修改。这不算错，但要知道它是这么表现的。

**顺序方案也可以提，但不推荐。** 可以按小说 ID 做一致性哈希路由，让同一本书的消息始终落到同一个消费者，天然串行。代价是扩容时哈希环变化、单个热门作者的消息挤在一个消费者上。版本号方案更简单，所以先选它。

---

## Q12 参考答案：对象生命周期与 Full GC 排查

**对象从 new 到回收，经过的区域按顺序说。**

1. **类加载检查。** 执行 new 时先在常量池找类的符号引用，检查类是否已加载、解析、初始化。类的元数据在元空间。
2. **分配内存。** 优先在当前线程的 TLAB 里指针碰撞分配，无锁。TLAB 不够就申请新的 TLAB，或者在 Eden 里用 CAS 分配。JIT 做逃逸分析后，没逃逸的对象会被标量替换，直接拆成局部变量放在栈帧里，根本不进堆。
3. **Eden 满了触发 Minor GC。** 复制算法，Eden 和 from 区里存活的对象拷到 to 区，年龄加一。栈上的引用变量、静态变量、JNI 引用这些 GC Roots 出发可达的才算存活。
4. **在两个 Survivor 之间来回。** 每熬过一次 Minor GC 年龄加一。
5. **进入老年代。** 年龄到阈值后晋升。
6. **老年代满了触发 Full GC。** 标记清除或标记整理，不可达的对象被回收。如果对象重写了 finalize，会先进 F-Queue 执行一次 finalize，有机会自救，但只有一次。

**直接进入老年代的情况，一共四种。**

|情况|触发条件|
|---|---|
|大对象|超过 PretenureSizeThreshold，只对 Serial 和 ParNew 生效。G1 下超过 Region 一半的对象进 Humongous 区，逻辑上属于老年代|
|年龄达到阈值|MaxTenuringThreshold 默认 15，CMS 默认 6|
|动态年龄判断|Survivor 里年龄小于等于某个值的对象总大小超过 Survivor 一半，大于等于这个年龄的对象全部晋升|
|空间分配担保|Minor GC 后存活对象放不下 to 区，直接晋升到老年代|

第三条最容易被漏掉，它意味着实际晋升年龄经常远小于 15，Survivor 太小的时候对象很快就进老年代。

**频繁 Full GC 的排查，我按三步走：先看日志确认原因，再看堆里是什么，最后定位代码。**

**第一步，GC 日志和 jstat。** JDK 8 用 PrintGCDetails 加 Xloggc，JDK 9 以后用 Xlog。用 jstat 每秒采样一次，看老年代使用率、Full GC 次数和耗时的走势。重点看两个东西。一是日志里的 GC 原因字段，Allocation Failure、Metadata GC Threshold、System.gc、G1 Humongous Allocation、to-space exhausted，每种原因对应不同的病。二是 Full GC 前后老年代的回收量。回收后老年代降得很少，说明对象确实活着，多半是泄漏。回收后降得很多，说明对象本该在年轻代就死，是晋升太快。

**第二步，按原因分流。**

- **泄漏或对象真活着。** 用 jmap 的 histo 看实例数和占用最大的类，注意带 live 参数本身会触发一次 Full GC，要先把这台机器从负载均衡摘掉。然后 dump 堆用 MAT 看支配树和泄漏嫌疑。常见元凶是静态集合无限增长、ThreadLocal 没 remove、本地缓存没设上限、连接没归还。
- **晋升太快。** 年轻代太小、Survivor 太小、动态年龄判断把对象提前推进老年代。调 Xmn、SurvivorRatio，看对象年龄分布。
- **大对象频繁分配。** 一次查出全表、导出整本书的章节、大字节数组，这类对象直接进老年代或 Humongous 区。G1 下 Humongous 分配会反复触发并发周期，回收不过来就退化成 Full GC。改成分页分批处理。
- **元空间满。** 动态生成类太多，CGLIB 代理、反射膨胀、脚本引擎。用 jstat 看已加载类的数量是否只增不减，检查 MaxMetaspaceSize。
- **显式调用 System.gc。** RMI 的分布式垃圾回收会定时调，堆外内存不够时 DirectByteBuffer 分配路径也会调。加 ExplicitGCInvokesConcurrent 让它变成并发回收而不是 Full GC，并给堆外内存设 MaxDirectMemorySize。
- **CMS 特有。** 并发模式失败和晋升失败，碎片化导致有空间但放不下，退化成带压缩的串行 Full GC。

**第三步，改完用同样的 GC 日志验证。** 对比调整前后 Full GC 频率和老年代曲线，不能凭感觉说修好了。

**结合项目说一句。** novel 的 Caffeine 缓存每个都设了 maximumSize，正文缓存 3000 条，这是防止本地缓存把老年代吃满的关键配置。hmall 的拦截器在 afterCompletion 里 remove ThreadLocal，防的也是同一类泄漏。

---

## 面试官点评

- **Q11 的核心是先讲"消息只带 ID"这个设计，再讲它为什么能扛住乱序和重复。** 然后主动承认多实例下的窗口，并给出版本号方案。坏消息无限重投这个问题，你的代码里是真实存在的，建议面试前先改掉，这样可以说成"我发现并修复了"。
- **Q12 的区分度在两处。** 一是动态年龄判断和空间分配担保，说明你不是只背了年龄 15。二是排查时先看 Full GC 前后的回收量来判断是泄漏还是晋升过快，这是有章法的思路，比罗列工具名更有说服力。

---

**下一轮，一道 novel 的分布式锁题加一道 MySQL 基础题：**

**Q13.** novel 里有一个基于 Redisson 的锁切面。锁的 key 是什么，加在哪个业务上？Redisson 的看门狗是怎么续期的，业务执行时间超过锁的过期时间会怎样？还有一个常被忽略的问题：锁在方法返回时释放，但事务在方法返回之后才提交，中间这个窗口会造成什么后果，怎么解决？

**Q14.** MVCC 在 InnoDB 里是怎么实现的？可重复读隔离级别下，普通 SELECT 和 `SELECT ... FOR UPDATE` 读到的数据为什么可能不一样？你在支付流程里用 FOR UPDATE 锁支付单，如果这条支付单还不存在，锁住的是什么？

我先看一下 novel 的锁切面和 hmall 支付服务里的行锁 SQL，再给参考答案。

我已核对完 novel 的锁切面、加锁的评论方法和评论表的唯一索引，以及 hmall 支付服务的两条 FOR UPDATE 语句和 biz_order_no 的唯一索引。下面是对照代码的参考答案，其中 Q14 顺带发现了你 hmall 里一个真实的并发窗口。

---

## Q13 参考答案：Redisson 锁切面

**锁加在哪，key 是什么。**

项目里只有一处用了这个切面，就是用户发表书评的 saveComment。业务规则是一个用户对一本书只能评论一次，代码是先查 count 再 insert，典型的先检查后写入，并发下会重复插入。锁的 key 由三段拼出来：固定前缀 Lock，注解上的 prefix 是 userComment，参数上的 SpEL 模板把 DTO 里的 userId 和 bookId 拼在一起。最终形如：

```
Lock::userComment::{userId}::{bookId}
```

粒度是"用户加书"，不同用户或不同书互不阻塞。注解的 isWait 默认 false，用的是不等待的 tryLock，拿不到直接抛业务异常。重复点击这种场景没必要排队，快速失败更合适。

**看门狗怎么续期。**

只有 tryLock 或 lock 不传 leaseTime 时看门狗才启动，传了就没有。流程分三步：

1. 加锁的 Lua 脚本把 key 建成 hash，field 是"客户端 UUID 冒号线程 ID"，value 是重入次数，过期时间默认 30 秒，对应 lockWatchdogTimeout。
2. 加锁成功后注册一个定时任务，每隔过期时间的三分之一，也就是 10 秒跑一次。Lua 脚本检查 hash 里还有没有自己的 field，有就 pexpire 重设 30 秒，没有就停止续期。
3. unlock 时先取消定时任务，再删 key。

所以业务跑超过 30 秒不会丢锁，看门狗一直续到业务结束。真正丢锁的情况是续不上：JVM 长时间 STW、进程被杀、和 Redis 之间网络断开。30 秒内没续上 key 就过期，另一个线程拿到锁，原线程醒来后两个人同时在临界区。原线程 unlock 时切面先判断 isHeldByCurrentThread，不是自己的锁就跳过，避免误删别人的锁，否则 Redisson 会抛 IllegalMonitorStateException。但临界区已经被穿透了，锁救不回来，只能靠数据库唯一键兜底。book_comment 表正好有 book_id 加 user_id 的唯一索引，这就是最后一道防线。

**锁在事务提交前释放的问题。**

先看代码：saveComment 没有加 @Transactional，三条 SQL 各自自动提交，insert 在方法返回前已经提交，unlock 在提交之后，所以当前代码不存在这个窗口。

但只要给这个方法加上事务，窗口立刻出现。锁切面和事务拦截器都是 AOP 通知，顺序决定谁在外层。事务拦截器默认优先级最低，切面没标 @Order 时也是最低，相对顺序取决于 Spring 收集通知的次序，实践中常常是事务在外层。那样的执行顺序是：开事务，拿锁，查 count 为 0，insert，释放锁，提交。线程 B 在"释放锁"和"提交"之间拿到锁，查 count 仍是 0，因为 A 还没提交，于是 B 也 insert。有唯一键就抛 DuplicateKeyException，接口报 500 而不是返回"已评论"。没有唯一键就是重复数据。

还有一层更隐蔽。可重复读下 ReadView 在事务里第一次快照读时建立。如果 B 的事务在 A 提交之前就已经开始并做过查询，即便 B 之后才拿到锁，B 的 count 查询也看不到 A 提交的行。所以锁不仅要在事务提交之后释放，还要在事务开始之前获取。

解决办法按推荐顺序：

1. **让锁切面在事务外层。** 给 LockAspect 标 @Order，值比事务拦截器小即可。这样顺序变成先拿锁，再开事务，提交完再放锁。
2. **不想改切面顺序，就在锁内部用 TransactionTemplate 手动开事务**，把事务边界收进锁边界之内。
3. **用 TransactionSynchronization 的 afterCompletion 释放锁**，事务提交或回滚之后再 unlock。
4. **无论哪种，唯一键都要留着**，并捕获 DuplicateKeyException 转成"已评论"的业务响应。锁提高的是并发下的成功率，唯一键才是正确性的底线。

**面试前建议改的一个小点。** Lock 注解的 failCode 默认是 OK，saveComment 没有覆盖，并发被拒的请求会抛一个错误码为 OK 的异常，前端拿到的提示是错的。改成 USER_COMMENTED 之类的码，面试时可以说成自己发现并修复的。

---

## Q14 参考答案：MVCC 与 FOR UPDATE

**InnoDB 的 MVCC 靠三样东西。**

1. **隐藏列。** 每行有 DB_TRX_ID 记录最后修改它的事务 ID，DB_ROLL_PTR 指向 undo log 里的上一个版本。没有主键时还有 DB_ROW_ID。
2. **undo log 版本链。** 每次更新先把旧版本写进 undo log，新行的回滚指针指向它。同一行的历史版本串成一条从新到旧的链。
3. **ReadView。** 快照读时生成，记录当时活跃的事务 ID 列表、其中最小的 ID、下一个将分配的 ID、以及自己的 ID。判断某个版本可不可见：修改它的事务是自己，可见。小于最小活跃 ID，说明早已提交，可见。大于等于下一个 ID，说明是快照之后才开始的，不可见。落在中间就看它在不在活跃列表，在就不可见。不可见就沿回滚指针往上找，直到找到可见的版本。

读已提交和可重复读的区别只在 ReadView 的生成时机。前者每条 SELECT 生成一个新的，所以看得到别人新提交的数据。后者事务里第一次快照读生成一次，之后一直复用，整个事务看到同一份快照。

**普通 SELECT 和 FOR UPDATE 为什么结果不一样。**

普通 SELECT 是快照读，走 MVCC，读 ReadView 决定的历史版本，不加锁。FOR UPDATE、LOCK IN SHARE MODE、UPDATE、DELETE 都是当前读，读最新已提交的版本并加锁。可重复读下另一个事务提交了一次更新，本事务的普通 SELECT 依然返回旧快照，紧接着一条 FOR UPDATE 却返回新值。这也是可重复读没有完全消除幻读的原因：快照读靠 MVCC 看不到新插入的行，当前读靠间隙锁挡住别人插入，两者在一个事务里混用，先快照读再当前读，就能看到快照里没有的行。

支付流程里用 FOR UPDATE 而不是普通查询，就是要拿到最新状态并锁住这一行，让并发的付款和关单在这里排队，后来者拿到锁时看到的是前一个人提交后的状态。

**支付单不存在时锁住的是什么。**

按业务订单号查，这一列在升级脚本里加了唯一索引。可重复读下，唯一索引等值查询命中记录时只加记录锁，不加间隙锁。没命中时加的是间隙锁，锁住该值在索引里本应落入的那段区间，两端是相邻的已有值。没有行，所以锁住的不是行，是一段空隙。读已提交下则什么都不锁。没有索引的话会全表扫描，把所有记录和间隙都锁上，等于锁表。

这带来三个后果：

|后果|原因|
|---|---|
|两个事务同时 FOR UPDATE 同一个不存在的值，都能成功|间隙锁之间互相兼容，它只挡插入，不挡另一个间隙锁|
|两个都成功后各自 INSERT，会死锁|插入要先拿插入意向锁，它和对方的间隙锁冲突，双方互等，InnoDB 检测到后回滚一个|
|值比索引里所有现有值都大时，锁住的是到正无穷的整段|业务订单号是雪花 ID 递增，新订单常落在尾部，锁住尾部间隙会挡住所有新支付单的插入，直到本事务提交|

**对应到项目里有一个真实的窗口，建议主动讲。** 代码注释写的是"付款和关单只能有一个成功"，这个互斥只在支付单已存在时成立。支付单不存在时，关单方法直接返回 true，事务提交后间隙锁就释放了。此时如果用户正在申请支付单，且申请流程已经校验过订单是未付款状态，它的插入只是被间隙锁暂时挡了一下，关单事务一提交就插进去了。结果是订单已关闭，却新生成了一张待支付的支付单。用户接着付款，支付单状态是待支付，校验通过，余额扣掉，支付单标记成功，通知交易服务。交易服务按"状态等于未付款"做条件更新，订单已经是已关闭，更新影响 0 行，日志记一句"忽略重复通知"。钱扣了，订单没了。

修法不复杂：关单时发现没有支付单，不要直接返回 true，而是插入一条状态为已关闭的支付单占位。之后申请支付单时，唯一索引拦下并发插入，幂等校验查到已关闭状态就抛"订单已关闭"，付款接口也会因为状态不是待支付而拒绝。这样"支付单一定存在"变成常态，行锁的互斥才真正成立。

---

## 面试官点评

- **Q13 先说"当前代码没有事务，所以窗口不存在"，再讲加了事务会怎样。** 以代码事实为准的回答比背通用结论可信得多。ReadView 在事务开始时建立这一层，能说出来是明显的区分点。
- **Q14 大多数人答到"间隙锁"就停了。** 能说出间隙锁互相兼容导致的死锁，以及把它对应到自己项目里"支付单不存在时互斥失效"，是把原理和业务串起来的表现。这个窗口建议面试前修掉，修完就是你的加分故事。

---

**下一轮，一道 AI 找书助手的题加一道 Spring 事务基础题：**

**Q15.** 你在 novel 里用 Spring AI 接了 4 个业务工具。模型是怎么决定调哪个工具、传什么参数的？如果模型编造了一个不存在的书名或者把 bookId 传成了字符串，你的代码会怎样？"加入书架前校验登录状态"这个校验是放在工具方法里，还是让模型来判断，为什么？另外语义找书的 Embedding 是什么时候算的，小说信息更新后向量要不要重算，kNN 检索用的是什么索引结构？

**Q16.** 我看你代码里 deleteBookChapter 写的是 `@Transactional(rollbackFor = Exception.class)`，而 updateBookChapter 只写了 `@Transactional`。这两种写法在行为上有什么区别？再列举几种 @Transactional 不生效或者不回滚的常见场景，并说说你怎么排查。

我已核对完 AI 找书助手的全部代码：4 个工具用 SafeFunctionCallback 包装成 FunctionCallback Bean，向量同步走 EmbeddingModel 写 ES，检索用 dense_vector 的 HNSW 索引。下面是对照代码的答案，Q15 里发现了两处真实问题。

---

## Q15 参考答案：Spring AI 找书助手

**模型怎么决定调哪个工具、传什么参数。**

模型本身不执行代码，它只是根据每个工具的 name、description 和入参的 JSON Schema 做决策。项目里这三样都显式给了:

- description 写清楚了工具的用途和取值,比如查榜工具直接列出 rankType 的三个合法值 visit、newest、update。
- 入参用 record 定义,字段上的 JsonPropertyDescription 是给模型看的说明,JsonProperty 的 required 标出必填。SafeFunctionCallback 构造时用 ModelOptionsUtils.getJsonSchema 把 record 转成 JSON Schema 交给模型。

模型读完用户的话,匹配到某个工具,按 Schema 生成一段 JSON 参数,框架把 JSON 反序列化成 record,再调用绑定的方法。所以工具描述写得越准,模型选得越对,这是 prompt 工程的一部分,不是靠代码逻辑保证的。

**模型编造书名或传错类型会怎样,分两种情况讲。**

第一,编造不存在的书或 ID。找书工具不接受书名,只接受自然语言描述,模型编的"书名"会被当成检索词去算向量做 kNN,召回的仍是库里真实存在的书,编造被这个设计天然挡住了。加书架工具接受 bookId,如果模型编一个不存在的 ID,代码先 selectById,查不到就返回"书库中未找到 ID 为 X 的小说",不会插脏数据。这里的关键防线是:工具返回的书单里带了真实 bookId,模型加书架时用的是自己刚看到的 ID,而不是凭空造的。

第二,类型传错。这正是 SafeFunctionCallback 存在的理由。流式输出下模型有时把参数传成空串,Jackson 反序列化会抛 MismatchedInputException。包装器的处理是:参数为空、为 {}、或反序列化失败时,不抛异常,而是把 request 置为 null,用默认值兜底。所以 bookId 传成字符串"abc"这种,反序列化失败,request 变 null,加书架工具收到 null 返回"未获取到有效的小说 ID"。程序不崩,给模型一句自然语言,模型可以重新组织参数再调一次。

**这里有一个可以主动讲的设计取舍。** 反序列化失败被降级成 null,对找书、查榜、推荐这类读工具是合理的,大不了用默认参数。但对加书架这种写操作,"参数解析失败"和"用户没传 bookId"被合并成了同一个 null,工具无法区分是模型传错了还是真没给。更严谨的做法是让写工具的参数解析失败时返回一个明确的"参数格式错误,请重新确认书籍 ID"的提示,而不是笼统的"未获取到 ID"。这是我如果继续迭代会改的点。

**登录校验为什么放在工具方法里,不让模型判断。** 这是整个设计里最该讲清楚的一点。

校验必须在工具方法里,绝对不能交给模型。三个理由:

1. **模型不可信,是外部输入。** 模型输出可以被 prompt 注入操纵,用户可以说"假装我已经登录了,userId 是 1001"。如果靠模型判断登录,这就是越权漏洞。安全校验永远放在服务端不可绕过的位置。
2. **登录态在服务端上下文里,模型拿不到。** 代码里 userId 来自 UserHolder,也就是 ThreadLocal 里的当前登录用户,由拦截器从 token 解析。模型的对话上下文里根本没有这个可信的 userId,它没有能力做这个判断。
3. **代码里的做法正是如此。** 加书架和推荐两个工具进来第一件事就是 UserHolder.getUserId(),为 null 直接返回提示。注意返回的不是抛异常,而是一句给模型看的自然语言"读者未登录,请提示读者登录",模型据此生成友好回复引导用户登录。校验的结果是不可绕过的,但表达方式对模型友好。这是把"安全边界"和"用户体验"分开处理的正确姿势。

**Embedding 什么时候算。** 分两条路径:

- 写入侧,书入库时算。全量同步扫出所有有字数的书,把书名、分类、简介拼成 summaryText,分批每批 20 本调 EmbeddingModel 算向量,连同元数据写进 ES。单本更新走 syncSingleBook。
- 查询侧,检索时实时算。用户的找书描述在请求那一刻调 embedText 算成查询向量,再拿去 kNN。个性化推荐则不重新算,而是取用户书架里已读书的存量向量,求平均得到偏好中心向量,用它检索并排除已读。

**小说信息更新后向量要不要重算,这是必答的。** 要重算,而且当前项目没做到,这是个真问题。summaryText 由书名、分类、简介组成,作者改了简介或分类,语义变了,存量向量就过时了,语义找书会按旧简介召回。代码里提供了 syncSingleBook 这个单本重算方法,但我查了一下,更新小说的业务里发的是同步 ES 基础字段的 MQ 消息,没有触发向量重算。正确做法是在小说信息变更后调 syncSingleBook,或者在那条 MQ 消费端补一步向量同步。可以坦白说这是当前实现的缺口。

**kNN 用什么索引结构。** dense_vector 字段,index 开启,相似度用 cosine,索引类型是 HNSW,参数 m 是 16,efConstruction 是 100。

HNSW 是分层可导航小世界图,一句话原理:构建时把向量组织成多层图,上层稀疏节点少用来快速跳转,下层稠密含全部节点,查询从顶层贪心找最近邻逐层下沉。它是近似最近邻 ANN,不保证百分百精确,用可控的精度损失换远高于暴力计算的速度。三个参数的含义:

|参数|含义|权衡|
|---|---|---|
|m = 16|每个节点的连接数|越大精度越高,内存和构建越慢|
|efConstruction = 100|构建时考察的候选数|越大图质量越好,建索引越慢|
|numCandidates|查询时每个分片的候选数|代码里设为 topK 的 10 倍且至少 50,越大召回越准,查询越慢|

代码里 numCandidates 取 topK 乘 10,是在召回质量和延迟之间的一个常见折中。

---

## Q16 参考答案:@Transactional 的回滚行为

**两种写法的区别。**

`@Transactional` 默认只在抛出 RuntimeException 和 Error 时回滚,遇到受检异常 checked exception 不回滚,事务照样提交。`@Transactional(rollbackFor = Exception.class)` 把回滚范围扩大到所有异常,包括受检异常。

所以 deleteBookChapter 无论抛什么异常都回滚,updateBookChapter 只在运行时异常时回滚。对这两个方法来说,内部调用的 MyBatis-Plus 方法抛的基本都是 DataAccessException 这类运行时异常,实际行为差别不大。但写法不统一是隐患:哪天有人在 updateBookChapter 里加一个抛受检异常的调用,比如手动 IO 或远程调用,异常抛出时前面的库更新已经提交,不会回滚,数据就不一致了。规范做法是统一加 rollbackFor = Exception.class,Alibaba 开发手册也是这么要求的。这一点可以说成"我注意到项目里写法不一致,建议统一"。

**@Transactional 不生效或不回滚的常见场景。**

1. **同类内部自调用。** 一个方法调用同类里另一个带 @Transactional 的方法,走的是 this 引用,不经过 Spring 代理,注解失效。因为事务是 AOP 代理实现的,只有从外部经过代理对象调用才会被增强。解法是把方法拆到另一个 Bean,或注入自己的代理,或用 AopContext.currentProxy()。
2. **异常被自己吞了。** 方法里 try-catch 把异常捕获没有重新抛出,事务管理器感知不到异常,正常提交。这是最高频的坑。
3. **抛的是受检异常但没配 rollbackFor。** 就是上面说的,默认不回滚受检异常。
4. **方法不是 public。** Spring 的事务代理默认只拦截 public 方法,加在 private、protected、包级方法上不生效。
5. **方法是 final 或类是 final。** CGLIB 代理靠继承生成子类重写方法,final 无法被重写,增强不上。
6. **目标对象没被 Spring 管理。** 自己 new 出来的对象,不是从容器拿的,没有代理。
7. **多线程调用。** 事务靠 ThreadLocal 绑定连接,新开线程拿不到当前事务,子线程的操作在事务之外。
8. **传播行为不匹配。** 比如内层用 REQUIRES_NEW 另起事务,外层回滚不影响已提交的内层;或者用了 NOT_SUPPORTED 挂起事务。
9. **数据库引擎不支持事务。** 表是 MyISAM,根本没有事务能力,注解无意义。
10. **异步注解叠加。** 方法上同时有 @Async,实际执行在另一个线程,同第 7 条。

**怎么排查,我按由外到内的顺序。**

1. **先确认代理有没有生成。** 打断点看注入的 Bean 是不是代理对象,类名带 EnhancerBySpringCGLIB 或 Proxy 说明被代理了。没有代理,注解从一开始就没生效。
2. **看是不是自调用。** 检查这个事务方法是不是被同类的其他方法直接调的。这个最常见,优先排。
3. **看异常有没有真的抛出来。** 检查方法里有没有 try-catch 吞掉异常,以及抛的是不是受检异常而没配 rollbackFor。
4. **看方法签名。** 是不是 public,类和方法是不是 final。
5. **开事务日志验证。** 把 org.springframework.transaction 的日志级别调到 DEBUG,能看到事务的开启、提交、回滚。如果日志里压根没有"Creating new transaction"这行,说明事务没进来,问题在代理或方法签名。如果有开启但没有回滚,问题在异常处理或 rollbackFor。
6. **确认传播行为和数据源。** 多数据源时检查用的连接是不是同一个,传播行为是不是被内层改了。

日志验证这一步最关键,它能直接把问题定位到"事务没开"还是"开了没回滚"两个方向,不用靠猜。

---

## 面试官点评

- **Q15 的高分点有三个。** 一是能讲清模型靠 name、description、Schema 做决策,而不是靠代码逻辑。二是登录校验必须在服务端、模型不可信这个安全意识,这是做 AI 应用最容易被忽视也最容易出事的地方。三是主动指出小说更新后向量没重算这个真实缺口,以及写工具把"解析失败"和"没传参"混成 null 的取舍。承认缺口比假装完美可信得多。
- **Q16 是八股,区分度在自调用和 rollbackFor 这两个高频实战坑,以及排查时用事务 DEBUG 日志先分流"没开"还是"没回滚"。** 能说出这个分流思路,面试官会知道你真排查过。

---

**下一轮,进入相对综合的设计题,也补一道计网基础:**

**Q17.** 假设 novel 首页推荐现在用 Caffeine 本地缓存,某个热门推荐位突然被大量请求击穿到数据库,你怎么排查是缓存击穿、穿透还是雪崩?这三者你分别怎么防?结合你项目里 Caffeine 加 Redis 两级缓存的结构说,如果做成两级,读写流程是怎样的,一致性怎么保证?

**Q18.** 从浏览器输入你 novel 的网址到页面显示,中间发生了什么?重点讲 DNS、TCP 三次握手、以及 HTTPS 的 TLS 握手大概流程。为什么要三次握手不是两次?

---


### Q17 参考答案:击穿、穿透、雪崩与两级缓存

**先讲怎么区分这三者,因为排查手段不同。**

三者的共同现象都是"数据库压力突增",但成因不同,看两个指标就能区分:请求的 key 是否存在、是否集中在少数 key。

|类型|特征|排查抓手|
|---|---|---|
|击穿|单个热点 key 过期瞬间,大量请求同时打到库,查的是同一个真实存在的 key|看 DB 慢查询日志里是不是集中在一个 key,时间点是否正好卡在该 key 的 TTL 到期|
|穿透|查的是库里根本不存在的 key,缓存永远 miss,每次都打库|看请求参数是不是大量非法或不存在的 ID,比如负数 ID、超大 ID,缓存命中率异常低|
|雪崩|大量 key 在同一时刻集体过期,或缓存整体宕机,请求全量打库|看 DB 压力是不是覆盖大量不同 key,时间点是否是某批缓存的统一过期时刻,或 Redis 是否刚重启|

题目说的是"某个热门推荐位被大量请求打到库",查的是同一个真实存在的 key,这是典型**击穿**。首页推荐 HOME_BOOK_CACHE 的 TTL 是 24 小时且 maxSize 只有 1,一旦这条过期或被淘汰,所有首页请求同一瞬间全部 miss,一起回源。

**三者分别怎么防。**

击穿,防的是"热点 key 过期瞬间的并发回源":

1. 回源加互斥锁,只放一个线程查库重建,其余等待或读旧值。项目里有现成的 Redisson 锁切面,可以复用。Caffeine 本身用 `get(key, mappingFunction)` 时对同一 key 的加载是串行的,天然有单飞效果,这点要说。
2. 热点数据逻辑不过期,value 里存一个逻辑过期时间,过期后异步刷新,读请求始终拿到旧值不阻塞。首页推荐这种能容忍短暂旧数据的场景很合适。

穿透,防的是"查不存在的 key":

1. 缓存空值,查库为 null 也缓存一个短 TTL 的空标记。项目里 Redis 缓存配了 disableCachingNullValues,也就是不缓存 null,这在防穿透上是个反向配置,面试可以点出来:它避免了空值占内存,但代价是不存在的 key 会反复打库,是个取舍。
2. 布隆过滤器,请求先过滤,一定不存在的直接拒绝。
3. 入口参数校验,非法 ID 直接挡掉,成本最低。

雪崩,防的是"大面积同时失效":

1. TTL 加随机抖动,避免同一批 key 同一秒过期。
2. 缓存高可用,Redis 做主从加哨兵或集群,别让整个缓存层单点宕机。
3. 服务端限流降级,用 Sentinel 兜底,缓存挂了也不让库被打死。hmall 里就用了 Sentinel。

**两级缓存的读写流程,以及为什么当前项目其实没做成两级。**

先说现状:项目把缓存按数据特征分开放,访问频繁且能容忍多实例短暂不一致的放 Caffeine 本地,比如首页推荐、章节信息;需要跨实例一致或数据量大的放 Redis,比如章节正文、用户信息。这是"分而治之",不是"同一份数据两级"。枚举里预留了 type=1 的两级能力,但没有一个缓存启用。

如果把首页推荐做成真正的两级 Caffeine 加 Redis,读写流程应该是:

读:

1. 先查 Caffeine,命中直接返回,这是最快的一级,扛住绝大多数流量。
2. Caffeine miss,查 Redis,命中则回填 Caffeine 再返回。
3. Redis 也 miss,加分布式锁回源查库,重建后写 Redis 再写 Caffeine,释放锁。锁保证击穿时只有一个实例查库。

写或失效:

1. 更新数据库。
2. 删除 Redis。
3. 删除本地 Caffeine,但这里有本地缓存的老问题:`@CacheEvict` 只能清当前实例,其他实例的 Caffeine 还是旧的。

**一致性怎么保证,这是两级缓存最难的地方。** 本地缓存的跨实例失效必须靠广播:

1. 发一条 MQ 广播消息或用 Redis 的 pub/sub,每个实例订阅,收到就清自己的 Caffeine。
2. 给本地缓存设较短 TTL 做兜底,即便广播丢了,最多旧 TTL 时长。
3. 本地缓存只放能容忍秒级不一致的数据,像首页推荐、榜单,绝不放余额、库存这种强一致数据。

一句话收尾:两级缓存是用"本地缓存的一致性难题"换"极致的读性能",所以只对读多写少、能容忍短暂旧数据的热点数据才值得做,首页推荐正合适,而正文、用户信息这些项目就没往本地放。

---

### Q18 参考答案:从输入网址到页面显示

**整体链路,我分七段说。**

1. **URL 解析。** 浏览器拆解协议、域名、端口、路径,先查强缓存,命中直接用本地副本,不发请求。
2. **DNS 解析。** 把域名转成 IP。查找顺序是:浏览器 DNS 缓存,操作系统缓存和 hosts 文件,本地 DNS 服务器,本地没有则递归查询,从根域名服务器到顶级域再到权威域名服务器,拿到 IP 逐级缓存。DNS 默认走 UDP 53 端口,报文超长时转 TCP。
3. **建立 TCP 连接。** 和服务器 IP 的目标端口做三次握手。
4. **TLS 握手。** HTTPS 在 TCP 之上再做 TLS 握手,协商加密。
5. **发 HTTP 请求。** 连接建好,浏览器发请求行、请求头、请求体。novel 是前后端分离,首页 HTML 加载后,里面的接口再发 Ajax 请求拿数据。
6. **服务器处理并响应。** 请求过网关、路由到服务、查缓存或库、返回 JSON 或 HTML。
7. **浏览器渲染。** 解析 HTML 构建 DOM,解析 CSS 构建 CSSOM,合成渲染树,布局,绘制,合成上屏。遇到 JS 可能阻塞解析,遇到图片等资源再发请求。用完四次挥手关连接,或 keep-alive 复用。

**TCP 三次握手。**

1. 客户端发 SYN,带自己的初始序列号 x,进入 SYN_SENT。
2. 服务端回 SYN + ACK,带自己的序列号 y,确认号 x+1,进入 SYN_RCVD。
3. 客户端回 ACK,确认号 y+1,进入 ESTABLISHED。服务端收到后也进入 ESTABLISHED。

**为什么是三次不是两次,核心是要双向确认收发能力,并防止历史连接。**

- TCP 是全双工,双方都要确认自己能收、能发。第一次握手让服务端确认"客户端能发、我能收";第二次让客户端确认"服务端能收也能发、我能收";但此时服务端还不知道"客户端能不能收到我的包",所以需要第三次,客户端回 ACK,服务端才确认"客户端能收"。两次握手缺了对客户端接收能力的确认。
- 更本质的理由是防止历史失效连接。假设只有两次:客户端发的一个早已滞留在网络里的旧 SYN 姗姗来迟到达服务端,服务端以为是新连接,直接建立并等待数据,而客户端根本没打算连,这个连接就空耗服务端资源。三次握手下,客户端对这个意外的 SYN+ACK 不会回 ACK,连接建不起来。
- 还有初始序列号的同步。双方都要把自己的 ISN 告诉对方并得到确认,三次正好让两个方向的序列号都完成一发一确认。

**TLS 握手,以 RSA 密钥交换为例讲经典流程,再点一句 1.3 的改进。**

1. 客户端发 ClientHello,带支持的 TLS 版本、加密套件列表、一个随机数。
2. 服务端回 ServerHello,选定版本和套件,发自己的随机数,并把证书发过来,证书里有服务器公钥。
3. 客户端验证证书,校验签发的 CA 是否可信、域名是否匹配、是否在有效期、是否被吊销。验证通过后生成一个预主密钥,用证书里的公钥加密发给服务端。
4. 双方各自用两个随机数加预主密钥,按相同算法算出对称的会话密钥。
5. 双方发 Finished 报文互相验证握手没被篡改,之后改用对称加密通信。

一句话概括设计思想:用非对称加密安全地交换出一个对称密钥,之后用对称加密传数据,因为非对称慢、对称快,兼顾安全和性能。

补充两点区分度:

- TLS 1.3 简化成一次往返,握手更快,且废弃了 RSA 密钥交换,强制用 ECDHE 这类前向安全的算法。前向安全的意思是,即便服务器私钥日后泄露,也无法解密之前抓包的历史流量,因为会话密钥不是用私钥直接传的。
- 证书验证解决的是"公钥到底是不是这个网站的"信任问题,靠 CA 签名的信任链,防的是中间人拿自己的公钥冒充服务器。

---

### 面试官点评

- **Q17 的高分点是先讲怎么区分三者再讲怎么防,而不是背定义。** 区分靠"key 存不存在"和"是否集中在少数 key"这两个维度,是有方法论的。结合项目最出彩的是两点:一是指出 Redis 配了 disableCachingNullValues,这在防穿透上是反向的,是个真实取舍;二是老实说项目其实没做成两级,只是分而治之,并讲清真做两级时本地缓存跨实例失效要靠广播。承认"没做"比硬编一个不存在的两级流程强得多。
- **Q18 是纯八股,区分度全在"为什么三次"。** 大多数人只会背"确认双向通信",能补上"防止历史失效连接空耗资源"和"同步初始序列号"才算答透。TLS 部分能点出 1.3 的前向安全,是明显的加分项。

---

**下一轮,回到 hmall 做一道链路设计题,再补一道并发基础:**

**Q19.** hmall 里支付成功后,pay-service 用本地消息表加 RabbitMQ 通知 trade-service 改订单状态。请完整讲一遍这条通知链路:消息表什么时候写、什么时候发、发失败怎么办、trade-service 消费时怎么保证幂等。如果 trade-service 一直消费失败,这条消息最终会怎样,你怎么兜底?

**Q20.** 线程池的 7 个核心参数分别是什么?一个任务提交进来,线程池处理它的完整决策流程是怎样的?为什么阿里手册不建议用 Executors 直接创建线程池?

你来帮我回答一下

Read pay-service local message table publisher and trade-service pay status listener

我已核对完 pay-service 的本地消息表全套代码:消息表实体、发布器、confirm/return 回调、定时重试,以及 trade-service 的消费监听器。这套实现相当完整,下面对照代码讲。

---

### Q19 参考答案:支付通知的可靠投递链路

**先给一张状态流转图,再逐段讲。** 消息表有三个状态:待发送 0、已发送 1、失败 2。

**第一步,什么时候写消息表。** 在 tryPayOrderByBalance 这个支付事务里,扣完余额、改完支付单状态之后,调 saveAndPublishAfterCommit。方法第一件事就是把消息以"待发送"状态 insert 进 pay_message 表。关键点:这条 insert 和支付业务在同一个事务里。这是本地消息表的核心思想,业务数据和消息记录同库同事务,要么一起成功要么一起回滚,不会出现"支付成功了但消息没记下"或"消息记了但支付回滚了"的中间态。它把"跨系统的分布式事务"转化成了"本地事务加最终一致的异步投递"。

**第二步,什么时候发。** 不是 insert 完立刻发,而是通过 TransactionSynchronization 注册一个 afterCommit 回调,等事务真正提交后才把消息发给 RabbitMQ。原因在代码注释里写得很清楚:如果提交前就发,消费者可能在支付状态还没提交时就来查,查到的是旧状态。这和 novel 里 MQ 同步 ES、缓存删除延迟到提交后是同一个设计原则,所有对外副作用都放到事务提交之后。

**第三步,发失败怎么办,这里有三重保障。**

1. **发布确认 confirm。** 开了 correlated 模式,消息发出后 Broker 会回 ack 或 nack。发送时把消息 id 放进 CorrelationData,回调里就知道是哪条。ack 说明 Broker 已持久化,把消息标记为"已发送";nack 说明 Broker 拒收,安排重试。
2. **退回 return。** 消息到了交换机但没有匹配队列会触发退回回调,从 header 里取 msgId,当成一次发送失败安排重试。
3. **定时重试兜底。** 一个每 5 秒跑的定时任务,扫描所有"待发送"且已到下次重试时间的消息重投。这是为了覆盖 confirm 回调丢失、服务发完就宕机这类情况,只要消息还是"待发送",最终一定会被这个任务捞起来重发。

重试用指数退避,5 秒、10 秒、20 秒逐次翻倍,避免下游抖动时高频冲击。有个并发细节值得讲:confirm 回调、return 回调、定时任务三方可能同时操作同一条消息,代码用 retry_count 当版本号做条件更新,谁先改成功谁生效,避免重试次数被重复累加。发送前先推后下次重试时间,也是防止发送耗时期间被下一轮扫描重复拿到。

**第四步,消费端怎么保证幂等,这是我最想讲的一点。** trade-service 的监听器只有一行:直接调 markOrderPaySuccess,不做"先查订单再改"。幂等靠的是带状态条件的 UPDATE:

UPDATE `order` SET status = 2, pay_time = now()
WHERE id = ? AND status = 1

只有当前是"未付款"状态才更新成功。重复投递的第二条消息进来,status 已经是已付款,更新影响 0 行,自然被忽略,日志记一句"已处理"。代码注释特意说明为什么不"先查再改":查询和更新之间有并发窗口,两个线程都查到未付款然后都去改,而条件更新把判断和更新合并成一条原子 SQL,没有窗口。这是用数据库的行锁加状态机代替了额外的去重表,轻量且正确。

**第五步,一直消费失败最终会怎样,怎么兜底。**

要分清"发不出去"和"消费不了"两个不同的失败,它们的兜底机制不一样:

- **发送侧失败**,也就是消息发不到 Broker。定时任务重试,超过最大 6 次后把消息标记为"失败"状态并打 error 日志告警,不再无限重试。这时消息停在失败状态,等人工介入或告警触发的补偿。
- **消费侧失败**,消息发出去了但 trade-service 处理不了。这就要看消费端的重试和死信配置了。这里我要如实说:trade-service 的这个 PayStatusListener 目前没有显式配置消费重试和死信队列,依赖 Spring AMQP 的默认行为。默认情况下消费抛异常会重新入队,一条坏消息可能反复重投。这是当前实现的一个缺口。

**我会怎么补消费侧的兜底:**

1. **配死信队列。** 消费端设置有上限的重试,比如 3 次,耗尽后消息进死信队列 DLQ,不再阻塞正常队列,由人工或补偿任务处理死信。
2. **对账兜底。** 这是最终防线,也和项目已有的对账逻辑呼应。pay-service 有 reconcilePayOrder,根据扣款流水补齐支付状态并补发通知。可以再加一个定时对账任务,扫描"支付单已成功但对应订单仍未付款超过一定时间"的记录,重新驱动通知或直接改订单状态。因为有扣款流水这个事实来源,即便消息彻底丢了,对账也能把状态补齐。
3. **消费端也要幂等前提下才能安全重试。** 好在上面的状态条件更新已经保证了幂等,所以无论重试还是对账补偿,重复执行都不会把订单改错,这是能放心做兜底的基础。

一句话收尾:本地消息表解决的是"支付成功后通知一定要发出去"的可靠投递,发送侧靠 confirm 加定时重试保证消息最终离开本服务,消费侧靠状态条件更新保证幂等、靠死信和对账保证最终一定被正确处理。发送和消费两侧都要有兜底,只做一侧是不完整的。

---

### Q20 参考答案:线程池

**7 个核心参数。**

1. **corePoolSize,核心线程数。** 常驻线程数,默认不会被回收。
2. **maximumPoolSize,最大线程数。** 线程池能创建的线程上限。
3. **keepAliveTime,空闲存活时间。** 超过核心数的线程空闲多久后被回收。
4. **unit,时间单位。** 配合上面那个用。
5. **workQueue,任务队列。** 存放等待执行的任务的阻塞队列。
6. **threadFactory,线程工厂。** 创建线程用,可以给线程命名、设为守护线程等,生产环境建议自定义以便排查。
7. **handler,拒绝策略。** 队列满且线程数达上限时,新任务怎么处理。

**任务提交进来的完整决策流程,这是必考点,顺序不能错。**

1. 当前线程数小于核心数,直接创建一个核心线程执行,即便有空闲线程也创建。
2. 达到核心数,任务放进工作队列排队等待。
3. 队列满了,且线程数还没到最大数,创建非核心线程执行。
4. 队列满了且线程数也到最大数,执行拒绝策略。

一句话记:先加核心线程,再入队,再加临时线程,最后拒绝。这里最容易被追问的一个反直觉点:队列没满时不会创建超过核心数的线程,也就是说"先塞满队列才扩线程"。所以如果用了无界队列,最大线程数这个参数永远不生效,线程数永远停在核心数,任务无限堆积在队列里,这正是下面第一个坑。

**4 种拒绝策略。**

|策略|行为|
|---|---|
|AbortPolicy|默认,直接抛 RejectedExecutionException|
|CallerRunsPolicy|让提交任务的线程自己执行,能减缓提交速度做反压|
|DiscardPolicy|静默丢弃新任务,不报错|
|DiscardOldestPolicy|丢掉队列里最老的任务,再尝试提交新任务|

**为什么阿里手册不建议用 Executors,核心是它们藏了资源耗尽的风险,而参数被隐藏后不易察觉。**

- **newFixedThreadPool 和 newSingleThreadExecutor** 用的是无界队列 LinkedBlockingQueue,不传容量默认 Integer.MAX_VALUE。任务堆积时队列无限增长,最大线程数形同虚设,最终 OOM。
- **newCachedThreadPool 和 newScheduledThreadPool** 的最大线程数是 Integer.MAX_VALUE,任务突增时可以创建近乎无限的线程,每个线程默认占用 1MB 栈空间,直接把内存或系统线程数打爆,同样 OOM。

根本问题是用 Executors 时这些危险参数被默认值藏起来了,写代码的人看不到边界在哪。手册要求用 ThreadPoolExecutor 的构造方法显式传参,好处是:强制你思考队列该多大、线程数上限多少、满了怎么拒绝,让资源边界显式可控,也便于按业务压测调优。生产环境还应该自定义 threadFactory 给线程起有意义的名字,出问题时看线程名就知道是哪个池的,配合监控暴露活跃线程数、队列长度这些指标。

**结合项目说一句。** 我项目里用到线程池的地方,比如 pay-service 的定时重试任务、novel 的向量同步批处理,如果要自定义异步线程池,会按业务类型区分是 CPU 密集还是 IO 密集来定核心数。CPU 密集型核心数设为核数加 1,IO 密集型可以设到核数的两倍左右,因为线程大部分时间在等 IO,可以多开。队列容量按业务能容忍的积压量设一个有界值,拒绝策略对支付这种重要任务用 CallerRunsPolicy 做反压,而不是直接丢。

---

### 面试官点评

- **Q19 是你项目里实现得最扎实的一块,高分点有四个。** 一是消息与业务同事务落库这个本地消息表的灵魂,要讲清它把分布式事务转成了本地事务加最终一致。二是发送延迟到 afterCommit 的原因。三是消费幂等用状态条件更新而不是先查再改,能说出"避免查改之间的并发窗口"是明显的深度。四是老实指出消费侧没配死信队列这个缺口,并用项目已有的对账逻辑做兜底收尾。承认缺口加上有对账兜底,比假装完美强得多。
- **Q20 是八股,区分度在两处。** 一是决策流程里"队列没满不会扩线程"这个反直觉点,以及它导致无界队列让最大线程数失效。二是能把"为什么不用 Executors"归到"危险参数被默认值隐藏、资源边界不可控"这个根因,而不只是背"可能 OOM"。

---

我们已经聊了 20 道题,覆盖了你两个项目的核心链路和主要基础知识点。**下一轮我打算收个尾,问一道开放题加一道你可能被问到的反问环节:**

**Q21.** 抛开具体实现,你这两个项目里,你觉得哪个技术决策是你最纠结、或者事后回看觉得可以做得更好的?说一个就行,重点讲你当时怎么权衡的,现在会怎么改。这道题我想看你的技术判断力和反思能力,没有标准答案。

**Q22.** 面试到这里,你有什么想问我的?可以问团队、技术栈、实习生的工作内容,或者任何你关心的。

要不要我继续帮你把这两道也答了,还是你想自己试着答一下这两道偏开放的?

你来帮我回答一下

这两道是开放题,答案要体现判断力和反思,不是背知识点。我基于我们这 20 道题里挖出来的真实设计,给你一个站得住的版本。

---

### Q21 参考答案:最纠结的技术决策

我选**下单链路用 Seata AT 强一致,而支付通知用本地消息表最终一致**这个决策。同一个项目里对两条跨服务链路用了两套完全不同的一致性方案,这是我当时最纠结、现在回看也最有讨论空间的地方。

**当时的权衡。** 下单要跨交易、商品、购物车三个库,我第一反应是"必须保证一致",所以上了 Seata 的 `@GlobalTransactional`,任一步失败整条链路回滚,写起来也直观,一个注解搞定。支付通知那条我却没用 Seata,而是手写了本地消息表加重试。当时的理由是:支付通知的下游只是改个订单状态,不需要"实时一致",允许秒级延迟,用最终一致更轻,也不用让支付事务被 Seata 的全局锁拖着。

**纠结点在于,这两个判断其实用了双重标准。** 下单我怕不一致就上了强一致,支付我figured可以容忍延迟就用最终一致。但仔细想,下单链路本身也没那么需要强一致。扣库存失败,让订单也失败、让用户重下一单,这在电商里是完全可接受的,不一定非要一个全局事务原子回滚。而 Seata AT 的代价不小:它在每条 SQL 前后拍快照写 undo_log,还要拿全局锁,下单是高频操作,这个开销和锁竞争会直接压到下单的吞吐上。

**现在会怎么改。** 我会考虑把下单也改成最终一致的思路,和支付通知统一。具体说,下单主链路里"创建订单"是核心,必须同步成功;而"清购物车、扣库存"可以拆成下单成功后发消息异步处理,扣库存失败就走补偿或人工介入,而不是让整个下单卡在一个分布式事务里回滚。这样下单接口更快、不依赖 Seata、也少了全局锁的竞争。代价是要接受"订单已创建但库存还没扣"的短暂中间态,需要前置的库存预扣或校验来兜底,别让用户下了单最后扣不到库存。

**但我也会保留 Seata 作为对比而不是全盘否定它。** Seata 的价值在于开发成本低、语义清晰,对一致性要求高又不追求极致性能的内部系统很合适。所以更准确的反思是:我当时是"下意识地"对下单用了强一致,而不是"权衡后"选的。如果重来,我会先问一句"这条链路到底能不能容忍最终一致",能容忍就统一用消息驱动,不能才上 Seata,而不是凭直觉一条用强一致一条用最终一致。

**一句话收尾:这个项目让我意识到,一致性方案的选择不该看"听起来重不重要",而该看"这条链路到底能容忍多大的不一致窗口"。这是我做完两个项目最大的一个认知升级。**

---

### Q22 参考答案:反问环节

反问不是走过场,面试官会通过你问什么判断你关不关心这份工作、想不想清楚了。我按优先级给你几个,挑两三个真正想知道的问,别一口气全抛出来。

**关于工作内容,优先问,最能体现你想干活。**

1. 实习生进来后大概会参与什么样的任务?是修 bug、写测试这类偏边缘的,还是能接触到需求开发甚至设计环节?我想知道能不能真正参与到业务里。
2. 团队目前的技术栈和我做的项目重合度高吗?如果有我不熟的东西,比如你们用的消息中间件或注册中心和我项目里不一样,一般期望多久上手?

**关于团队和成长,体现你在意长期。**  
3. 团队的代码评审和上线流程是怎样的?我做项目时习惯先对齐规范再动手,想了解你们对代码质量和规范的要求。  
4. 实习生有没有 mentor 带?遇到问题一般是自己查还是有人可以随时问?

**关于业务和技术方向,体现你有大局观。**  
5. 团队现在最主要的技术挑战或者正在攻的方向是什么?比如是在做性能优化、架构升级,还是支撑新业务快速迭代?

**一个可以收尾的、体现诚意的问题。**  
6. 如果我拿到这个实习,入职前您建议我重点补哪块?我可以提前准备,尽快能上手帮上忙。

**三个要避开的雷区,面试前记住:**

- 别一上来只问薪资、能不能转正、加不加班。这些可以关心,但放最后,或者留到 HR 环节,技术面上先问技术和工作。
- 别问"你们公司是做什么的"这种官网就能查到的,显得没做功课。
- 别说"没什么想问的",这几乎等于告诉面试官你对这份工作没那么在意。哪怕真没想好,也从上面挑一个问。

---

### 面试官整体点评(模拟收尾)

万同学,聊完这 22 道题,我给你几个真实的反馈:

**优点。**

- 两个项目是真做过的,不是简历工程。我追问到 Seata AT 回滚镜像、本地消息表的 confirm 回调、HNSW 参数这种细节,你的代码里都有对应实现和注释,经得起深挖。这在校招里很难得。
- 你在几个地方主动承认了实现的缺口,比如向量更新后没重算、消费端没配死信队列、支付单不存在时的互斥失效。**承认缺口并给出改法,比假装完美可信得多**,这是我给你加分的地方。

**要改进的。**

- 开场自我介绍太短,项目归属感的措辞要注意,别让人觉得是"整理"别人的代码。
- 回答有时候容易停在"我这么做了",要多往前走一步讲"为什么这么做、不这么做会怎样、还有别的方案吗"。你其实有这个深度,但需要主动输出,别等面试官追问。

**面试前必做的三件事(基于我们发现的真实问题):**

1. 修掉支付单不存在时关单直接返回 true 导致的"订单已关闭却能新建支付单"窗口,改成插占位支付单。修完就是你的加分故事。
2. 给消费端补死信队列配置,或至少能讲清楚现在没有、打算怎么补。
3. 统一 `@Transactional` 的 rollbackFor 写法,顺手把评论锁的 failCode 默认 OK 改掉。