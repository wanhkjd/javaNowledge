# 万守洋

**求职意向：Java 后端开发实习生** ｜ **电话**：178-3933-0662 ｜ **邮箱**：3599808381@qq.com  
**年级**：2027 届本科 ｜ **实习时长**：每周 5 天，可实习 3~6 个月，随时到岗  

---

## 🎓 教育背景

**河南农业大学** ｜ 软件工程（工学学士） ｜ 2023.09 – 2027.06（2027 届）
- **主修课程**：数据结构与算法、计算机网络、操作系统、数据库系统概论、Java 程序设计、分布式系统架构、软件工程
- **综合素养**：通过大学英语四/六级（CET-4/6），具备扎实的计算机学科功底与良好的英文技术文档阅读能力

---

## 💻 专业技能

- **Java 核心与并发编程**：熟练掌握 Java 核心语法、集合框架（深入理解 HashMap、ConcurrentHashMap 源码与扩容机制）、I/O 模型与反射机制；深入理解 JUC 并发编程，熟练运用线程池、CAS、AQS 核心原理、`ThreadLocal` 内存模型与生命周期清理规范，具备并发安全编码能力。
- **JVM 底层原理**：熟悉 JVM 运行时数据区（堆、方法区、虚拟机栈等）、类加载机制与双亲委派模型；理解垃圾收集算法（标记-清除/复制/标记-整理）、GC 分代收集理论及常见收集器（G1、CMS）工作原理与调优参数。
- **主流开发框架与组件**：熟练掌握 Spring Boot 3、Spring MVC、MyBatis、MyBatis-Plus；深入理解 Spring IoC/DI、AOP 原理、Bean 生命周期与声明式事务传播机制；具备基于 `spring.factories` / SPI 封装自定义通用 Starter 组件、全局统一异常处理与统一响应模型的能力。
- **微服务与分布式体系**：熟练掌握 Spring Cloud Alibaba 体系（Nacos 注册配置中心、Gateway 统一网关、OpenFeign 声明式调用、Sentinel 流量防卫、Seata 分布式事务）；具备微服务拆分、网关动态路由热更新、跨服务全链路上下文透传与服务容错降级落地经验。
- **数据库与 SQL 优化**：熟练使用 MySQL 8.x，深入理解 InnoDB 存储引擎架构、B+ 树索引底层结构与最左前缀原则、事务 ACID 特性、MVCC 多版本并发控制与锁机制（行锁、间隙锁、临键锁）；具备通过 `EXPLAIN` 分析执行计划进行慢 SQL 优化与索引优化的实战经验；掌握基于 ShardingSphere-JDBC 的水平分表方案。
- **分布式缓存与高并发**：熟练掌握 Redis 核心数据结构及底层实现、RDB/AOF 持久化与主从哨兵机制；熟练掌握缓存穿透、击穿、雪崩高并发问题解决方案；具备基于 Caffeine + Redis 构建本地与分布式两级缓存、Redisson 结合 AOP/SpEL 落地声明式分布式锁的实战经验。
- **消息中间件与搜索引擎**：熟练使用 RabbitMQ 消息中间件，掌握消息可靠性投递（Publisher Confirm、Return 机制、消费端手动 ACK）、死信队列与延迟队列，掌握基于 Spring 事务同步器（`TransactionSynchronizationManager`）实现“事务提交后发 MQ”的数据一致性保障；熟练掌握 Elasticsearch 8.x + IK 分词器进行多字段布尔检索、高亮搜索、DSL 构建与增全量索引同步。
- **高可用与前沿技术**：熟悉 Sentinel 接口限流与 IP 级防刷规则、XXL-JOB 分布式任务调度；了解 Spring AI 与大模型 Function Calling、向量数据库检索技术；熟练使用 Git、Maven、Docker、Linux 常用命令，严格遵循《阿里巴巴 Java 开发手册》规约。

---

## 🚀 项目经历

### 1. 万佳商城微服务电商系统）
- **项目描述**：基于 Spring Cloud Alibaba 架构构建的高可用 B2C 微服务电商系统，划分为商品服务（Item）、购物车服务（Cart）、交易服务（Trade）、支付服务（Pay）、用户服务（User）及统一网关（Gateway）等核心微服务，完整支撑商品检索、购物车结算、并发下单、库存扣减、支付回调与超时关单等电商全链路闭环。
- **核心职责与技术亮点**：
  1. **微服务架构重构与动态网关路由**：主导单体架构向微服务解耦演进，梳理服务业务边界并完成 5 大微服务拆分；基于 **Spring Cloud Gateway** 搭建统一接入网关，结合 **Nacos 配置监听机制**（`DynamicRoutLoader`）实现网关路由规则秒级热更新，避免修改路由规则导致网关重启。
  2. **统一网关鉴权与全链路上下文透传**：在 Gateway 中设计全局鉴权过滤器（`AuthGlobalFilter`），结合 JWT 与自定义路径白名单完成请求验签与无状态登录校验；下游服务通过 `UserInfoInterceptor` 拦截解析请求头写入 **ThreadLocal**（`UserContext`），并在 `afterCompletion` 阶段强制清理防止线程池复用导致的内存泄漏与数据串用；配置 **OpenFeign RequestInterceptor** 实现跨服务 RPC 调用全链路用户身份透传。
  3. **公共组件沉淀与统一工程规范**：抽取 `hm-common` 通用基础设施模块，基于 `spring.factories` 自动装配 MVC、MyBatis-Plus、RabbitMQ 序列化等基础配置；封装业务通用异常基类体系（`CommonExceptionAdvice`）与标准化响应对象 `R`，统一全链路异常拦截与错误码规范，减少 40% 以上的跨服务冗余代码。
  4. **高并发下单与 Seata 分布式事务一致性**：负责核心订单创建链路，针对跨服务操作（交易服务写单、购物车服务清空、商品服务扣库存）引入 **Seata AT 模式**（`@GlobalTransactional`），保障跨微服务的数据最终一致性；库存扣减采用数据库行级原子更新语句（`update item set stock = stock - #{num} where id = #{id} and stock >= #{num}`），在保证高性能的同时杜绝高并发超卖现象。
  5. **微服务容错降级与高可用治理**：集成 **Sentinel** 针对 OpenFeign 远程调用配置流控与熔断降级规则，基于 `FallbackFactory` 封装统一降级兜底逻辑（如商品微服务异常时返回降级数据），有效阻断依赖故障传播，杜绝微服务雪崩效应。
  6. **支付状态机设计与 RabbitMQ 异步解耦**：设计支付单幂等校验与基于状态机乐观锁的支付状态流转机制（`eq(id).in(status, NOT_COMMIT, WAIT_BUYER_PAY)`），防止重复支付；支付成功后通过 **RabbitMQ**（Direct Exchange）异步广播支付成功事件通知交易服务修改订单状态，并配置 Publisher Confirm 与消费者手动 ACK，实现交易与支付链路的高性能解耦与消息零丢失。

---

### 2. Novel 互动小说阅读与创作平台
- **项目描述**：面向读者、签约作者与平台管理三端的高并发互动小说阅读平台，包含小说门户、作家工作台与运营管理后台，提供智能书库检索、多级缓存小说阅读、多端权限控制、作家章节发布审核、智能书童 AI 伴读等核心功能，支撑百万级小说与亿级章节内容的高效访问与创作。
- **核心职责与技术亮点**：
  1. **Caffeine + Redis 两级高性能缓存架构**：针对首页推荐、分类列表、排行榜及小说详情等千万级读请求场景，基于 **Spring Cache + Caffeine + Redis** 设计本地+分布式两级缓存方案；针对热门榜单与小说详情设置差异化 TTL 并加入随机过期抖动，结合空值缓存（`unless = "#result == null"`）防范缓存穿透、击穿与雪崩；高频读接口平均响应耗时由 45ms 降至 3ms，数据库读压力降低 90% 以上。
  2. **声明式分布式锁与横向越权防御**：基于 **Redisson + 自定义 `@Lock` / `@Key` 注解 + Spring AOP + SpEL** 封装声明式分布式锁组件，实现对锁前缀、动态参数提取与超时等待的灵活配置，解决用户高频评论、章节并发创建等写操作的幂等性与并发安全问题；设计作家作品归属权切面校验，严密防范跨作者查看、修改、删除作品章节的横向越权安全隐患。
  3. **ES 8.x 全文检索与 Spring 事务一致性同步**：集成 **Elasticsearch 8.x Java Client + IK 中文分词器**，构建多字段布尔复合检索（`BoolQuery`）、关键字高亮展示与动态分页；基于 **RabbitMQ** 实现作品与章节变更的实时增量同步，创新性利用 Spring **`TransactionSynchronizationManager` 的 `afterCommit` 回调机制**，确保“MySQL 本地事务完全提交成功后再投递 MQ 消息”，彻底消除并发下消费端读取脏数据的隐患；配合 **XXL-JOB** 分布式定时任务执行全量数据定时校验与平滑重建。
  4. **Sentinel 流量防护与动态 SQL 注入防御**：基于 **Sentinel** 自定义 `FlowLimitInterceptor` 拦截器，配置系统全局 QPS 匀速排队限流（2000 QPS）与 IP 维度热点参数流控规则（单 IP 50 次/秒、1000 次/分钟），有效防御恶意爬虫与暴力刷量；针对复杂多条件动态排序场景，设计 `SortOrderValidationAspect` 切面拦截 Mapper 参数，基于白名单机制清洗排序字段与方向，根绝动态 `ORDER BY` 带来的 SQL 注入风险。
  5. **Spring AI 智能书童与向量语义找书创新**：基于 **Spring AI (`ChatClient`) + Function Calling** 打造智能交互书童，封装语义找书、榜单查询、一键加入书架等 4 大业务 Tools；结合 **Elasticsearch 8 向量检索** 实现自然语言语义匹配（如“找一本杀伐果断的修仙种田文”）；采用响应式 **`Flux<String>`** 结合 Unicode 码点分块实现流式打字机输出，彻底解决多字节 Emoji 截断乱码问题。
  6. **海量章节内容分表架构演进**：针对小说章节正文（`book_content`）文本数据量巨大、单表性能急剧下降的问题，设计并落地基于 **ShardingSphere-JDBC** 的水平分表方案（按 `chapter_id % 10` 拆分为 10 张物理分表），有效控制单表数据规模，保障亿级文本数据的高效检索与持久化。

---

## 🌟 自我评价

- **基础扎实，注重规范**：软件工程科班背景，深入理解 Java 核心、JUC 并发编程、JVM 内存模型与 MySQL 底层原理；严格遵守《阿里巴巴 Java 开发手册》，具备良好的工程分层思想、设计模式运用与代码洁癖。
- **工程能力强，业务视野开阔**：具备完整的微服务电商与高并发内容平台实操经验，对分布式事务、两级缓存、异步解耦、接口防护与大模型 AI 落地等核心技术有深入的实战思考与排错能力。
- **自驱力强，拥抱前沿**：保持高强度技术输入与复盘总结习惯，对 Spring Boot 3、Java 21、Spring AI 等前沿技术保持敏锐触觉；具备良好的沟通表达、团队协同与抗压能力。